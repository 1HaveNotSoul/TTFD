# ✅ ФИНАЛЬНАЯ СВОДКА: Система кодов привязки Discord-Telegram

## 📊 Статус: Готово к тестированию

**Дата:** 09.02.2026  
**Коммиты:** d9cc663, 604cf07

---

## 🔧 Исправленные проблемы

### 1. ❌ Команда `/getcode` не регистрировалась
**Причина:** Импорт `asyncpg` в `discord_link_code.py`, хотя модуль не использовался  
**Решение:** Удалён импорт `asyncpg` и неиспользуемые модули

### 2. ❌ 403 Forbidden при синхронизации команд
**Причина:** Guild sync требует специальные права бота  
**Решение:** 
- Используем **global sync** как основной метод
- Guild sync как дополнительный (fallback)
- Команды появятся в течение 1 часа через global sync

### 3. ❌ Ошибка "Must be 2000 or fewer in length"
**Причина:** Слишком длинные сообщения автообновлений  
**Решение:** Автоматическая разбивка длинных сообщений на части

---

## 📝 Изменённые файлы

### `discord-bot/py/discord_link_code.py`
- Удалён импорт `asyncpg`
- Удалены неиспользуемые импорты (`os`, `string`)

### `discord-bot/py/bot.py`
- Изменён метод синхронизации команд
- Добавлен global sync как основной
- Guild sync как дополнительный с обработкой ошибок

### `discord-bot/py/updates_system.py`
- Добавлена проверка длины сообщения (лимит 2000 символов)
- Автоматическая разбивка на части если слишком длинное

---

## 🎯 Как работает система

### Discord бот (генерирует код)

```
Пользователь: /getcode
    ↓
Бот генерирует код (6 символов, криптографически безопасный)
    ↓
Код сохраняется в json/user_data.json
    {
        "link_code": {
            "code": "ABC123",
            "created_at": "2026-02-09T12:00:00",
            "expires_at": "2026-02-09T12:03:00",
            "used": false
        }
    }
    ↓
Код отправляется в ЛС пользователю
```

### Telegram бот (использует код)

```
Пользователь: /code ABC123
    ↓
Бот читает discord-bot/json/user_data.json
    ↓
Проверки:
    ✓ Код существует?
    ✓ Код не истёк? (3 минуты)
    ✓ Код не использован?
    ↓
Помечает код как использованный
    {
        "link_code": {
            "code": "ABC123",
            "used": true,
            "used_at": "2026-02-09T12:01:00",
            "telegram_id": "123456789"
        }
    }
    ↓
Сохраняет привязку в обеих БД:
    - Discord: telegram_link = "123456789"
    - Telegram: discord_link = "987654321"
```

---

## 🚀 Что дальше

### 1. Дождаться деплоя на Railway
Railway автоматически задеплоит изменения из GitHub (1-2 минуты)

### 2. Проверить логи Railway (Discord Bot)

**Должны увидеть:**
```
✅ Команды привязки через код зарегистрированы (/getcode, /checklink, /unlink)
✅ Синхронизировано X slash команд с Discord (global sync)
⏰ Команды появятся в течение 1 часа
```

**НЕ должно быть:**
```
❌ No module named 'asyncpg'
❌ 403 Forbidden (error code: 50001): Missing Access
❌ Must be 2000 or fewer in length
```

### 3. Подождать до 1 часа
Global sync обновляет команды до 1 часа. Если guild sync сработал - команды появятся мгновенно.

### 4. Протестировать в Discord
```
/getcode → Код отправлен в ЛС
/checklink → Проверить статус привязки
/unlink → Отвязать Telegram
```

### 5. Протестировать в Telegram
```
/code ABC123 → ✅ Аккаунты привязаны!
/checklink → Проверить статус привязки
/unlink → Отвязать Discord
```

---

## ⚠️ Важные замечания

### Путь к Discord БД в Telegram боте
**Текущий путь:** `../../discord-bot/json/user_data.json`

**Проблема:** На Railway структура папок может быть другой!

**Решения:**
1. Использовать переменную окружения `DISCORD_DB_PATH`
2. Использовать PostgreSQL вместо JSON (рекомендуется)

### Альтернатива: PostgreSQL

Если JSON не работает на Railway, можно:

1. Создать таблицу `link_codes` в PostgreSQL:
```sql
CREATE TABLE link_codes (
    code VARCHAR(6) PRIMARY KEY,
    discord_id VARCHAR(20) NOT NULL,
    created_at TIMESTAMP NOT NULL,
    expires_at TIMESTAMP NOT NULL,
    used BOOLEAN DEFAULT FALSE,
    used_at TIMESTAMP,
    telegram_id VARCHAR(20)
);
```

2. Оба бота будут читать/писать в одну таблицу
3. Не нужно читать файлы друг друга

---

## 📋 Команды для пользователей

### Discord

| Команда | Описание |
|---------|----------|
| `/getcode` | Получить код для привязки Telegram |
| `/checklink` | Проверить статус привязки |
| `/unlink` | Отвязать Telegram аккаунт |

### Telegram

| Команда | Описание |
|---------|----------|
| `/code <КОД>` | Привязать Discord через код |
| `/checklink` | Проверить статус привязки |
| `/unlink` | Отвязать Discord аккаунт |

---

## 🔍 Диагностика проблем

### Команда не появляется в Discord

**Проверить:**
1. Логи Railway - есть ли ошибки регистрации?
2. Прошёл ли global sync успешно?
3. Прошло ли достаточно времени? (до 1 часа)

**Решение:**
- Подождать 1 час для global sync
- Проверить права бота в Discord
- Перезапустить бот на Railway

### Telegram не находит Discord БД

**Проверить:**
1. Путь к файлу: `../../discord-bot/json/user_data.json`
2. Существует ли файл на Railway?
3. Есть ли права на чтение?

**Решение:**
- Использовать PostgreSQL вместо JSON
- Добавить переменную окружения `DISCORD_DB_PATH`
- Проверить структуру папок на Railway

### Код не работает

**Проверить:**
1. Код правильный? (6 символов, заглавные)
2. Код не истёк? (3 минуты)
3. Код не использован?

**Решение:**
- Получить новый код: `/getcode`
- Проверить логи обоих ботов
- Проверить что БД синхронизированы

---

## 📊 Технические детали

### Генерация кода
- Длина: 6 символов
- Алфавит: `ABCDEFGHJKMNPQRSTUVWXYZ23456789` (без похожих символов)
- Метод: `secrets.choice()` (криптографически безопасный)

### Время жизни кода
- Создание: `datetime.now()`
- Истечение: `datetime.now() + timedelta(minutes=3)`
- Проверка: `expires_at < datetime.now()`

### Синхронизация команд
- **Global sync:** Работает везде, обновляется до 1 часа
- **Guild sync:** Работает на конкретном сервере, обновляется мгновенно
- **Fallback:** Если guild sync не работает - используется global sync

---

## ✅ Чеклист готовности

- [x] Убран импорт asyncpg
- [x] Изменён метод синхронизации команд
- [x] Добавлен fallback для guild sync
- [x] Исправлена ошибка длинных сообщений
- [x] Залито на GitHub
- [ ] Дождаться деплоя на Railway
- [ ] Проверить логи Railway
- [ ] Подождать до 1 часа (global sync)
- [ ] Протестировать `/getcode` в Discord
- [ ] Протестировать `/code` в Telegram

---

**Следующий шаг:** Дождаться деплоя на Railway и проверить логи!
