# ⚡ БЫСТРЫЙ СТАРТ: СИНХРОНИЗАЦИЯ БАЛАНСА

**Что сделано:** Ранги и баланс теперь синхронизируются между Telegram и Discord

---

## 🎯 ЧТО НУЖНО СДЕЛАТЬ

### 1. Discord Bot - Интегрировать wrapper

Откройте `TTFD-Discord/py/bot.py` (строки 15-30)

**Найдите:**
```python
if database_url:
    from database_postgres import db
else:
    from database import db
```

**Замените на:**
```python
if database_url:
    from database_postgres import db as local_db
    from database_sync_wrapper import create_sync_wrapper
    db = create_sync_wrapper(local_db)
else:
    from database import db as local_db
    from database_sync_wrapper import create_sync_wrapper
    db = create_sync_wrapper(local_db)
```

### 2. Telegram Bot - Интегрировать wrapper

Откройте `TTFD/telegram-bot/database.py` (в самый конец файла)

**Добавьте:**
```python
# Синхронизация баланса
try:
    from database_sync_wrapper import create_sync_wrapper
    db = create_sync_wrapper(db)
    print("✅ Синхронизация баланса включена")
except Exception as e:
    print(f"⚠️  Синхронизация: {e}")
```

### 3. Деплой

```bash
# Discord Bot - задеплойте на Railway вручную
# Telegram Bot
cd TTFD/telegram-bot
git add .
git commit -m "Интеграция синхронизации баланса"
git push
```

---

## ✅ ПРОВЕРКА

После запуска ботов вы должны увидеть:

**Discord:**
```
✅ Синхронизация баланса включена (Discord)
✅ Unified Database подключена (Discord)
```

**Telegram:**
```
✅ Синхронизация баланса включена (Telegram)
✅ Unified Database подключена
```

---

## 📊 РЕЗУЛЬТАТ

✅ Ранги синхронизированы (F, E, D, C, B, A, S)  
✅ Баланс синхронизирован (единые монеты)  
✅ Пользователи видят одинаковые данные на всех платформах

---

**Готово!** 🎉
