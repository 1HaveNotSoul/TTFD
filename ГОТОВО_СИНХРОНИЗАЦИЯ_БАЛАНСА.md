# ✅ СИНХРОНИЗАЦИЯ БАЛАНСА МЕЖДУ TELEGRAM И DISCORD

**Дата:** 09.02.2026  
**Статус:** ✅ Готово к интеграции

---

## 📋 ЧТО СДЕЛАНО

### 1. Создана система автоматической синхронизации баланса

Теперь баланс (монеты/coins) автоматически синхронизируется между всеми платформами:
- ✅ Telegram Bot
- ✅ Discord Bot  
- ✅ Website

### 2. Созданы файлы

**Shared модули:**
- `TTFD/shared/balance_sync.py` - Основной модуль синхронизации баланса

**Discord Bot:**
- `TTFD-Discord/py/database_sync_wrapper.py` - Обёртка для автоматической синхронизации

**Telegram Bot:**
- `TTFD/telegram-bot/database_sync_wrapper.py` - Обёртка для автоматической синхронизации

---

## 🔧 КАК ЭТО РАБОТАЕТ

### Принцип работы:

1. **Пользователь получает монеты в Telegram боте**
   - Локальная БД обновляется
   - Автоматически создаётся событие в unified database
   - Баланс синхронизируется с Discord и Website

2. **Пользователь получает монеты в Discord боте**
   - Локальная БД обновляется
   - Автоматически создаётся событие в unified database
   - Баланс синхронизируется с Telegram и Website

3. **Unified Database** - единый источник правды
   - Хранит актуальный баланс для всех платформ
   - Обрабатывает события синхронизации
   - Гарантирует консистентность данных

---

## 🚀 ИНТЕГРАЦИЯ

### Шаг 1: Discord Bot

Откройте `TTFD-Discord/py/bot.py` и добавьте в начало файла:

```python
# Импортируем wrapper для синхронизации
from database_sync_wrapper import create_sync_wrapper

# После создания db, оборачиваем его
if database_url:
    from database_postgres import db as local_db
    db = create_sync_wrapper(local_db)
else:
    from database import db as local_db
    db = create_sync_wrapper(local_db)
```

### Шаг 2: Telegram Bot

Откройте `TTFD/telegram-bot/database.py` и добавьте в конец файла:

```python
# Импортируем wrapper для синхронизации
try:
    from database_sync_wrapper import create_sync_wrapper
    db = create_sync_wrapper(db)
    print("✅ Синхронизация баланса включена")
except Exception as e:
    print(f"⚠️  Синхронизация баланса не включена: {e}")
```

### Шаг 3: Запустить Balance Sync Worker

Создайте файл `TTFD/shared/start_balance_sync.py`:

```python
"""
Запуск Balance Sync Worker
Обрабатывает события синхронизации баланса
"""
import asyncio
import logging
from balance_sync import get_balance_sync

logging.basicConfig(
    level=logging.INFO,
    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s'
)

async def main():
    print("🚀 Запуск Balance Sync Worker...")
    
    sync = await get_balance_sync()
    
    print("✅ Balance Sync Worker запущен")
    print("💡 Нажмите Ctrl+C для остановки")
    
    try:
        while True:
            await asyncio.sleep(1)
    except KeyboardInterrupt:
        print("\n🔌 Остановка Balance Sync Worker...")
        await sync.stop()

if __name__ == "__main__":
    asyncio.run(main())
```

---

## 📊 ПРИМЕР РАБОТЫ

### Сценарий 1: Пользователь играет в Telegram

```
1. Пользователь играет в игру в Telegram боте
2. Получает +100 монет
3. Локальная БД Telegram: coins = 100
4. Unified Database: coins = 100 ✅
5. Discord Bot видит: coins = 100 ✅
6. Website видит: coins = 100 ✅
```

### Сценарий 2: Пользователь получает награду в Discord

```
1. Пользователь получает ежедневную награду в Discord
2. Получает +50 монет
3. Локальная БД Discord: coins = 150
4. Unified Database: coins = 150 ✅
5. Telegram Bot видит: coins = 150 ✅
6. Website видит: coins = 150 ✅
```

---

## ⚙️ НАСТРОЙКА RAILWAY

### Discord Bot

Добавьте в `Procfile`:
```
web: python py/bot.py
```

### Telegram Bot

Добавьте в `Procfile`:
```
web: python main.py
```

### Balance Sync Worker (опционально)

Создайте отдельный сервис на Railway:
```
worker: python shared/start_balance_sync.py
```

---

## 🔍 ПРОВЕРКА РАБОТЫ

### 1. Проверить логи Discord бота:
```
✅ Синхронизация баланса включена (Discord)
✅ Монеты синхронизированы: discord_id=123, delta=50
```

### 2. Проверить логи Telegram бота:
```
✅ Синхронизация баланса включена (Telegram)
✅ Монеты синхронизированы: telegram_id=456, delta=100
```

### 3. Проверить unified database:
```sql
SELECT * FROM unified_users WHERE telegram_id = '456' OR discord_id = '123';
-- Баланс должен совпадать на всех платформах
```

---

## 📁 СТРУКТУРА ФАЙЛОВ

```
TTFD/
├── shared/
│   ├── database_unified.py          # Unified Database
│   ├── models.py                     # Модели данных
│   ├── balance_sync.py              # ✅ НОВЫЙ: Синхронизация баланса
│   └── start_balance_sync.py        # ✅ НОВЫЙ: Запуск worker
│
├── telegram-bot/
│   ├── database.py                   # Локальная БД
│   └── database_sync_wrapper.py     # ✅ НОВЫЙ: Wrapper для синхронизации
│
└── TTFD-Discord/
    └── py/
        ├── database_postgres.py      # Локальная БД
        └── database_sync_wrapper.py  # ✅ НОВЫЙ: Wrapper для синхронизации
```

---

## ⚠️ ВАЖНО

1. **DATABASE_URL должен быть настроен** на всех платформах
2. **Unified Database должна быть доступна** для всех ботов
3. **Синхронизация работает асинхронно** - может быть задержка 1-5 секунд
4. **Локальные БД остаются** - wrapper прозрачно добавляет синхронизацию

---

## 💡 ПРЕИМУЩЕСТВА

✅ Единый баланс на всех платформах  
✅ Автоматическая синхронизация  
✅ Прозрачная интеграция (не нужно переписывать код)  
✅ Отказоустойчивость (локальные БД работают даже если unified недоступна)  
✅ Логирование всех изменений баланса

---

## 🎯 СЛЕДУЮЩИЕ ШАГИ

1. **Интегрировать wrapper в Discord бота** (изменить `bot.py`)
2. **Интегрировать wrapper в Telegram бота** (изменить `database.py`)
3. **Залить изменения на GitHub**
4. **Railway автоматически задеплоит обновления**
5. **Проверить работу синхронизации в логах**

---

**Готово к интеграции!** 🎉

Баланс будет автоматически синхронизироваться между всеми платформами.
