# 🧪 Руководство по тестированию дизайна

## Проблема

Стили не применяются на сайте после обновления.

## Решение

### 1. Перезапусти сервер

**Важно!** Flask кеширует статические файлы. Нужно перезапустить сервер:

```bash
# Останови текущий процесс (Ctrl+C)
# Затем запусти заново:
python main.py
```

### 2. Очисти кеш браузера

После перезапуска сервера:

**Chrome/Edge:**
- `Ctrl + Shift + Delete`
- Выбери "Изображения и файлы, сохраненные в кеше"
- Нажми "Удалить данные"

**Или просто:**
- `Ctrl + F5` (жесткая перезагрузка страницы)

### 3. Проверь тестовую страницу

Открой в браузере:
```
http://localhost:5000/test-design
```

**Что ты должен увидеть:**
- ✅ Фиолетовый градиентный фон
- ✅ Белые карточки с тенями
- ✅ Градиентные кнопки (фиолетовая, розовая, синяя)
- ✅ Красивые формы с рамками
- ✅ Сообщения об успехе (зеленое) и ошибке (красное)

**Если НЕ видишь:**
- ❌ Страница белая или без стилей
- ❌ Кнопки обычные, без градиентов
- ❌ Нет фонового градиента

**Значит проблема в кешировании!**

### 4. Проверь другие страницы

После очистки кеша проверь:
- http://localhost:5000/ - Главная
- http://localhost:5000/settings - Настройки
- http://localhost:5000/games - Игры
- http://localhost:5000/leaderboard - Лидеры

## Что было сделано

### ✅ Добавлено версионирование CSS

Все страницы теперь загружают CSS с версией:
```html
<link rel="stylesheet" href="/static/css/main.css?v=2.0">
```

Это заставляет браузер загрузить новую версию файла.

### ✅ Создана тестовая страница

`/test-design` - специальная страница для проверки всех элементов дизайна.

### ✅ Все .md файлы перенесены в папку md/

Теперь документация в отдельной папке.

## Структура стилей

```
static/
  css/
    main.css      # Основные стили (используется на 10 страницах)
    snake.css     # Стили для игры змейка (не используется, можно удалить)
```

## Проверка работы CSS

### Способ 1: Через браузер

1. Открой любую страницу (например, `/settings`)
2. Нажми `F12` (открыть DevTools)
3. Перейди на вкладку "Network" (Сеть)
4. Перезагрузи страницу (`Ctrl + R`)
5. Найди файл `main.css?v=2.0`
6. Проверь:
   - Статус должен быть `200 OK`
   - Size не должен быть `(from cache)`

### Способ 2: Прямой доступ

Открой в браузере:
```
http://localhost:5000/static/css/main.css
```

Ты должен увидеть содержимое CSS файла.

## Если стили всё равно не работают

### Проверь 1: Файл существует
```bash
cd TTFD-Website
dir static\css\main.css
```

Должен показать файл.

### Проверь 2: Flask настроен правильно

В `web.py` должно быть:
```python
app = Flask(__name__)
```

Flask автоматически обслуживает папку `static/`.

### Проверь 3: Путь в HTML правильный

В любом HTML файле должно быть:
```html
<link rel="stylesheet" href="/static/css/main.css?v=2.0">
```

**Важно:** путь начинается с `/static/`, а не `static/`

### Проверь 4: Сервер запущен

```bash
python main.py
```

Должно быть:
```
✅ Используется PostgreSQL (или JSON)
 * Running on http://127.0.0.1:5000
```

## Быстрое решение

Если ничего не помогает:

1. **Останови сервер** (`Ctrl + C`)
2. **Очисти кеш браузера** (`Ctrl + Shift + Delete`)
3. **Закрой все вкладки** с сайтом
4. **Запусти сервер** (`python main.py`)
5. **Открой в режиме инкогнито** (`Ctrl + Shift + N`)
6. **Перейди на** `http://localhost:5000/test-design`

## Ожидаемый результат

### До (без стилей):
- Белый фон
- Черный текст
- Обычные кнопки
- Нет теней
- Нет градиентов

### После (со стилями):
- 🎨 Фиолетовый градиентный фон
- ✨ Белые карточки с тенями
- 💫 Градиентные кнопки
- 🎯 Плавные анимации
- 📱 Адаптивный дизайн

## Контрольный список

- [ ] Сервер перезапущен
- [ ] Кеш браузера очищен
- [ ] Тестовая страница открыта
- [ ] Видны градиенты и стили
- [ ] Все страницы работают
- [ ] Анимации плавные

## Если всё работает

Поздравляю! 🎉 Теперь у тебя:
- ✅ Единый дизайн на всех страницах
- ✅ Современный UI с градиентами
- ✅ Плавные анимации
- ✅ Адаптивный дизайн

## Дополнительно

### Изменение цветов

Если хочешь изменить цвета, отредактируй `static/css/main.css`:

```css
/* Найди эти строки и измени цвета */
background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
```

### Добавление новых стилей

Добавляй новые стили в конец `main.css`:

```css
/* Мои кастомные стили */
.my-class {
    /* твои стили */
}
```

### Версионирование

При изменении CSS увеличивай версию:
```html
<!-- Было -->
<link rel="stylesheet" href="/static/css/main.css?v=2.0">

<!-- Стало -->
<link rel="stylesheet" href="/static/css/main.css?v=2.1">
```

---

**Дата:** 29 января 2026  
**Версия CSS:** 2.0  
**Статус:** ✅ Готово к тестированию
