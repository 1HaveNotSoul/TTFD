# 🔄 Git Hook для автоматических обновлений

Автоматическое добавление изменений в Discord при каждом коммите.

## 🎯 Как это работает

1. **Делаешь коммит** в Git
2. **Git hook автоматически:**
   - Берёт сообщение коммита
   - Добавляет в `json/auto_update.json`
   - Устанавливает `enabled: true`
3. **При следующем запуске бота:**
   - Бот отправляет сообщение в Discord
   - Канал: `1466923990936326294`

## 🚀 Установка

### Способ 1: Автоматическая установка (рекомендуется)

```bash
УСТАНОВИТЬ_GIT_HOOK.bat
```

### Способ 2: Ручная установка

1. Скопируй файл `.git/hooks/post-commit`
2. Сделай его исполняемым (на Linux/Mac):
   ```bash
   chmod +x .git/hooks/post-commit
   ```

## 📝 Использование

### Обычный коммит

```bash
git add .
git commit -m "feat: добавлена новая команда !test"
git push
```

**Результат:**
```
✅ Добавлено обновление: добавлена новая команда !test
📊 Всего изменений: 1
```

### Префиксы коммитов

#### ✅ Добавляются в обновления:

- `feat:` - Новая функция
- `fix:` - Исправление бага
- `perf:` - Улучшение производительности
- `security:` - Безопасность

**Примеры:**
```bash
git commit -m "feat: добавлена команда !weather"
git commit -m "fix: исправлена ошибка в !profile"
git commit -m "perf: оптимизирована база данных"
```

#### ❌ НЕ добавляются в обновления:

- `docs:` - Документация
- `chore:` - Рутинные задачи
- `test:` - Тесты
- `style:` - Форматирование
- `refactor:` - Рефакторинг

**Примеры:**
```bash
git commit -m "docs: обновлена документация"
git commit -m "chore: обновлены зависимости"
git commit -m "test: добавлены тесты"
```

### Коммит без префикса

Если не указать префикс - изменение всё равно добавится:

```bash
git commit -m "улучшена система рангов"
```

**Результат:**
```
✅ Добавлено обновление: улучшена система рангов
```

## 🔧 Настройка

### Изменить префиксы

Отредактируй `discord-bot/git_update_handler.py`:

```python
# Префиксы которые НЕ добавляются
IGNORE_PREFIXES = [
    'docs:',
    'chore:',
    'test:',
]

# Префиксы которые ДОБАВЛЯЮТСЯ
INCLUDE_PREFIXES = [
    'feat:',
    'fix:',
    'perf:',
]
```

### Изменить канал обновлений

Отредактируй `discord-bot/py/updates_system.py`:

```python
UPDATES_CHANNEL_ID = 1466923990936326294  # Твой канал
```

## 📊 Примеры работы

### Пример 1: Добавление функции

```bash
git add .
git commit -m "feat: добавлена система достижений"
git push
```

**Вывод:**
```
═══════════════════════════════════════════════════════════
🔄 Git Update Handler
═══════════════════════════════════════════════════════════
📝 Сообщение коммита: feat: добавлена система достижений
✅ Добавлено обновление: добавлена система достижений
📊 Всего изменений: 1

✅ Обновление добавлено!
💡 При следующем запуске бота оно будет отправлено в Discord
═══════════════════════════════════════════════════════════
```

**Файл `json/auto_update.json`:**
```json
{
  "enabled": true,
  "changes": [
    "добавлена система достижений"
  ]
}
```

### Пример 2: Несколько коммитов

```bash
git commit -m "feat: добавлена команда !weather"
git commit -m "fix: исправлена ошибка в !daily"
git commit -m "docs: обновлена документация"
```

**Файл `json/auto_update.json`:**
```json
{
  "enabled": true,
  "changes": [
    "добавлена команда !weather",
    "исправлена ошибка в !daily"
  ]
}
```

Обрати внимание: `docs:` не добавился!

### Пример 3: Запуск бота

После коммитов запускаешь бота:

```bash
ЗАПУСК.bat
```

**Логи:**
```
🔄 Проверка автообновления...
🔄 Обнаружено автообновление, отправка уведомления...
✅ Уведомление об обновлении отправлено
✅ Автообновление выполнено успешно
```

**Discord канал `1466923990936326294`:**
```
🎉 ОБНОВЛЕНИЕ 2.1

📦 Текущая версия: 2.1
📅 Последнее обновление: 05.02.2026 | 12:00 МСК

✨ СПИСОК ИЗМЕНЕНИЙ:
• добавлена команда !weather
• исправлена ошибка в !daily
```

**Файл `json/auto_update.json` после отправки:**
```json
{
  "enabled": false,
  "changes": []
}
```

## ❓ Частые вопросы

### Q: Hook не работает?

**A:** Проверь:
1. Файл `.git/hooks/post-commit` существует
2. На Linux/Mac: файл исполняемый (`chmod +x`)
3. Python доступен из командной строки

### Q: Изменение не добавилось?

**A:** Проверь:
1. Префикс коммита (может быть в игнорируемых)
2. Логи после коммита
3. Файл `json/auto_update.json`

### Q: Как отключить hook?

**A:** Удали или переименуй файл:
```bash
mv .git/hooks/post-commit .git/hooks/post-commit.disabled
```

### Q: Можно ли добавить изменение вручную?

**A:** Да, отредактируй `json/auto_update.json`:
```json
{
  "enabled": true,
  "changes": [
    "моё изменение"
  ]
}
```

### Q: Как изменить версию?

**A:** Добавь в `json/auto_update.json`:
```json
{
  "enabled": true,
  "version": "3.0",
  "changes": [...]
}
```

### Q: Дубликаты не добавляются?

**A:** Да, hook проверяет дубликаты автоматически.

## 🧪 Тестирование

### Тест 1: Простой коммит

```bash
echo "test" > test.txt
git add test.txt
git commit -m "feat: тестовое изменение"
```

**Ожидаемый результат:**
```
✅ Добавлено обновление: тестовое изменение
```

### Тест 2: Игнорируемый коммит

```bash
git commit -m "docs: обновлена документация"
```

**Ожидаемый результат:**
```
ℹ️ Коммит игнорируется (префикс документации/тестов)
```

### Тест 3: Проверка файла

```bash
cat discord-bot/json/auto_update.json
```

**Ожидаемый результат:**
```json
{
  "enabled": true,
  "changes": [
    "тестовое изменение"
  ]
}
```

## 💡 Советы

### ✅ Хорошие сообщения коммитов:

```bash
git commit -m "feat: добавлена команда !weather для прогноза погоды"
git commit -m "fix: исправлена ошибка с дублированием монет"
git commit -m "perf: оптимизирована загрузка профилей"
```

### ❌ Плохие сообщения коммитов:

```bash
git commit -m "фикс"
git commit -m "обновление"
git commit -m "изменения"
```

### 💡 Рекомендации:

- Используй строчные буквы (для красивого шрифта в Discord)
- Будь конкретным
- Описывай ЧТО изменилось, а не КАК

## 🔄 Workflow

### Обычный процесс разработки:

```bash
# 1. Вносишь изменения в код
# 2. Коммитишь с понятным сообщением
git add .
git commit -m "feat: добавлена новая команда"

# 3. Hook автоматически добавляет в auto_update.json
# 4. Пушишь на GitHub
git push

# 5. Перезапускаешь бота (локально или на Railway)
# 6. Бот автоматически отправляет обновление в Discord
```

### Накопление изменений:

```bash
# Делаешь несколько коммитов
git commit -m "feat: команда !test1"
git commit -m "feat: команда !test2"
git commit -m "fix: ошибка в !profile"

# Все добавляются в auto_update.json
# При следующем запуске бота - все отправятся одним сообщением
```

## 📁 Файлы системы

```
TTFD/
├── .git/
│   └── hooks/
│       └── post-commit              # Git hook
├── discord-bot/
│   ├── git_update_handler.py        # Обработчик обновлений
│   ├── json/
│   │   └── auto_update.json         # Файл обновлений
│   └── py/
│       └── updates_system.py        # Система отправки
├── УСТАНОВИТЬ_GIT_HOOK.bat          # Установщик
└── GIT_HOOK_ИНСТРУКЦИЯ.md           # Эта инструкция
```

## ✅ Чеклист

### Установка:
- [ ] Запустил `УСТАНОВИТЬ_GIT_HOOK.bat`
- [ ] Hook установлен в `.git/hooks/post-commit`
- [ ] Сделал тестовый коммит
- [ ] Изменение добавилось в `auto_update.json`

### Использование:
- [ ] Делаю коммиты с префиксами
- [ ] Проверяю `auto_update.json` после коммита
- [ ] Перезапускаю бота
- [ ] Проверяю Discord канал

---

✅ **ГОТОВО!**

Теперь каждый коммит автоматически добавляется в обновления Discord бота!

---

**Дата:** 05.02.2026  
**Автор:** Kiro AI Assistant  
**Версия:** 1.0
