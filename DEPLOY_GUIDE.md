# 🚀 Деплой на Render.com через GitHub

## Шаг 1: Загрузка на GitHub

### 1.1 Создай репозиторий на GitHub
1. Зайди на https://github.com
2. Нажми "New repository" (зелёная кнопка)
3. Название: `TTFD-Website` (или любое другое)
4. Сделай репозиторий **Private** (чтобы никто не видел токены)
5. НЕ добавляй README, .gitignore или лицензию
6. Нажми "Create repository"

### 1.2 Загрузи код на GitHub

Открой терминал в папке проекта и выполни:

```bash
# Инициализируй git (если ещё не сделано)
git init

# Добавь все файлы
git add .

# Сделай первый коммит
git commit -m "Initial commit"

# Добавь удалённый репозиторий (замени YOUR_USERNAME и YOUR_REPO)
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git

# Загрузи код
git push -u origin main
```

**ВАЖНО:** Убедись, что файл `.env` НЕ загружен на GitHub! Он должен быть в `.gitignore`

### 1.3 Создай .gitignore (если его нет)

Создай файл `.gitignore` в корне проекта:

```
.env
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.so
*.egg
*.egg-info/
dist/
build/
```

## Шаг 2: Деплой на Render

### 2.1 Создай Web Service
1. Зайди на https://dashboard.render.com
2. Нажми "New +" → "Web Service"
3. Подключи свой GitHub аккаунт (если ещё не подключен)
4. Выбери репозиторий `TTFD-Website`
5. Нажми "Connect"

### 2.2 Настрой сервис

**Name:** `ttfd-bot` (или любое другое)

**Region:** Frankfurt (EU Central) - ближе к России

**Branch:** `main`

**Root Directory:** оставь пустым

**Runtime:** `Python 3`

**Build Command:**
```
pip install -r requirements.txt
```

**Start Command:**
```
python main.py
```

### 2.3 Настрой переменные окружения

Нажми "Advanced" → "Add Environment Variable" и добавь:

| Key | Value |
|-----|-------|
| `DISCORD_TOKEN` | Твой токен Discord бота |
| `GUILD_ID` | ID твоего Discord сервера |
| `WEB_PORT` | `10000` |
| `SECRET_KEY` | Любая случайная строка (например: `my_secret_key_12345`) |

### 2.4 Выбери план

- **Free Plan** - бесплатно, но засыпает после 15 минут неактивности
- **Starter Plan** ($7/месяц) - работает 24/7

Для начала выбери Free.

### 2.5 Деплой

Нажми "Create Web Service" и жди 5-10 минут.

## Шаг 3: Проверка

После успешного деплоя:

1. Render даст тебе URL типа: `https://ttfd-bot.onrender.com`
2. Открой этот URL в браузере
3. Должен открыться твой сайт!

## Шаг 4: Обновление кода

Когда захочешь обновить сайт:

```bash
git add .
git commit -m "Описание изменений"
git push
```

Render автоматически задеплоит новую версию!

## ⚠️ Важные моменты

### Проблема с Free планом
- Бот засыпает после 15 минут неактивности
- Первый запрос после сна занимает ~30 секунд
- Решение: используй сервис типа UptimeRobot для пинга каждые 5 минут

### Логи
- Смотри логи в Render Dashboard → Logs
- Там увидишь ошибки если что-то не работает

### База данных
- Сейчас используется JSON файл
- На Free плане файлы удаляются при каждом деплое
- Для продакшена лучше использовать PostgreSQL (можно добавить в Render)

## 🔧 Troubleshooting

### Ошибка "Application failed to respond"
- Проверь, что `WEB_PORT=10000` в переменных окружения
- Render использует порт 10000 по умолчанию

### Бот не запускается
- Проверь логи в Render Dashboard
- Убедись, что все переменные окружения заполнены
- Проверь, что токен Discord правильный

### Сайт открывается, но бот оффлайн
- Проверь токен Discord
- Убедись, что у бота включены все Intents в Discord Developer Portal

## 📞 Нужна помощь?

Пиши: [@bxdsun](https://t.me/bxdsun)
