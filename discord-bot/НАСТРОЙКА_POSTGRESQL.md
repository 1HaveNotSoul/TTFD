# 🗄️ Настройка PostgreSQL для Discord бота

## 📋 Содержание

1. [Зачем нужен PostgreSQL](#зачем-нужен-postgresql)
2. [Создание базы данных](#создание-базы-данных)
3. [Установка зависимостей](#установка-зависимостей)
4. [Настройка переменных окружения](#настройка-переменных-окружения)
5. [Миграция данных](#миграция-данных)
6. [Обновление кода](#обновление-кода)
7. [Тестирование](#тестирование)
8. [Решение проблем](#решение-проблем)

---

## 🎯 Зачем нужен PostgreSQL

### Проблема с JSON файлами:
- ❌ Данные теряются при каждом деплое
- ❌ Файловая система эфемерная на Railway/Render
- ❌ Нет резервных копий
- ❌ Медленная работа с большими данными

### Преимущества PostgreSQL:
- ✅ Данные сохраняются навсегда
- ✅ Автоматические бэкапы
- ✅ Быстрые запросы
- ✅ Масштабируемость
- ✅ Надёжность

---

## 🚀 Создание базы данных

### Вариант 1: Railway (Рекомендуется)

1. **Открой проект на Railway**
   - Перейди на https://railway.app
   - Открой свой проект с ботом

2. **Добавь PostgreSQL**
   ```
   New → Database → Add PostgreSQL
   ```

3. **Получи DATABASE_URL**
   - Открой PostgreSQL сервис
   - Вкладка "Variables"
   - Скопируй значение `DATABASE_URL`

### Вариант 2: Render

1. **Создай базу данных**
   - Dashboard → New → PostgreSQL

2. **Настрой базу**
   - Name: `ttfd-discord-db`
   - Region: выбери ближайший
   - Plan: Free

3. **Получи DATABASE_URL**
   - Открой созданную базу
   - Скопируй "Internal Database URL"

### Вариант 3: Локальное тестирование

```bash
# Установи PostgreSQL
# Windows: https://www.postgresql.org/download/windows/
# Mac: brew install postgresql
# Linux: sudo apt install postgresql

# Создай базу данных
createdb ttfd_discord

# DATABASE_URL будет:
postgresql://username:password@localhost:5432/ttfd_discord
```

---

## 📦 Установка зависимостей

### 1. Обнови requirements.txt

Добавь в `requirements.txt`:
```
psycopg2-binary==2.9.9
```

### 2. Установи локально

```bash
pip install psycopg2-binary
```

### 3. Для Railway/Render

Зависимости установятся автоматически при деплое.

---

## ⚙️ Настройка переменных окружения

### 1. Локально (.env файл)

Создай или обнови `.env`:
```env
# Discord Bot Token
DISCORD_TOKEN=твой_токен

# PostgreSQL Database URL
DATABASE_URL=postgresql://user:pass@host:port/database
```

### 2. На Railway

```
Project → Variables → New Variable

Name: DATABASE_URL
Value: (скопируй из PostgreSQL сервиса)
```

### 3. На Render

```
Environment → Add Environment Variable

Key: DATABASE_URL
Value: (Internal Database URL)
```

---

## 🔄 Миграция данных

### Шаг 1: Подготовка

Убедись что:
- ✅ PostgreSQL база создана
- ✅ DATABASE_URL установлен
- ✅ psycopg2-binary установлен
- ✅ JSON файлы с данными существуют

### Шаг 2: Запуск миграции

```bash
# Локально
python migrate_to_postgres.py

# Или через py
python py/migrate_to_postgres.py
```

### Шаг 3: Проверка

Скрипт покажет:
```
📦 Пользователей в JSON: 4
🗄️  Пользователей в PostgreSQL: 0

🚀 НАЧИНАЕМ МИГРАЦИЮ
➕ Добавление 739566637400981506: XP 320, Coins 1868
➕ Добавление 979427916742008832: XP 75, Coins 0
...

✅ Мигрировано: 4
⏭️  Пропущено: 0
❌ Ошибок: 0
```

---

## 🔧 Обновление кода

### Вариант 1: Автоматическое переключение (Рекомендуется)

Файл `py/bot.py` уже настроен на автоматическое переключение:

```python
# В начале файла
from database import db  # Автоматически выберет PostgreSQL если DATABASE_URL установлен
```

Файл `py/database_postgres.py` содержит fallback:
```python
try:
    if os.getenv('DATABASE_URL') and PSYCOPG2_AVAILABLE:
        db = PostgresDatabase()
        print("✅ Используется PostgreSQL")
    else:
        raise ValueError("PostgreSQL не настроен")
except Exception as e:
    print(f"⚠️ PostgreSQL недоступен: {e}")
    print("⚠️ Используется JSON файл")
    from database import db
```

### Вариант 2: Явное использование PostgreSQL

Если хочешь явно использовать PostgreSQL:

```python
# py/bot.py
from database_postgres import db  # Всегда PostgreSQL
```

---

## ✅ Тестирование

### 1. Локальное тестирование

```bash
# Запусти бота
python main.py

# Проверь логи
✅ Discord Bot: Используется PostgreSQL
✅ Таблицы PostgreSQL инициализированы
✅ Бот успешно запущен!
```

### 2. Тестирование команд

В Discord:
```
!profile - проверь свой профиль
!balance - проверь баланс
!daily - получи награду
!dice - сыграй в кубик
```

### 3. Проверка данных

Данные должны сохраниться после:
- Перезапуска бота
- Деплоя на сервер
- Обновления кода

---

## 🐛 Решение проблем

### Проблема: "psycopg2 не установлен"

**Решение:**
```bash
pip install psycopg2-binary
```

### Проблема: "DATABASE_URL не установлен"

**Решение:**
1. Проверь `.env` файл
2. Проверь переменные на Railway/Render
3. Перезапусти бота

### Проблема: "Ошибка подключения к PostgreSQL"

**Решение:**
1. Проверь DATABASE_URL (должен начинаться с `postgresql://`)
2. Проверь что база данных запущена
3. Проверь firewall/network настройки

### Проблема: "Данные не мигрировались"

**Решение:**
1. Проверь что JSON файлы существуют
2. Запусти миграцию снова
3. Проверь логи на ошибки

### Проблема: "Бот использует JSON вместо PostgreSQL"

**Решение:**
1. Убедись что DATABASE_URL установлен
2. Убедись что psycopg2-binary установлен
3. Проверь логи при запуске бота
4. Перезапусти бота

---

## 📊 Структура таблиц

### Таблица `users`
```sql
id TEXT PRIMARY KEY              -- Discord User ID
username TEXT                    -- Имя пользователя
xp INTEGER                       -- Опыт
coins INTEGER                    -- Монеты
rank_id INTEGER                  -- ID ранга (1-20)
daily_streak INTEGER             -- Серия дней
last_daily TIMESTAMP             -- Последняя награда
games_played INTEGER             -- Игр сыграно
games_won INTEGER                -- Игр выиграно
daily_tasks JSONB                -- Ежедневные задания
achievements JSONB               -- Достижения
inventory JSONB                  -- Инвентарь
game_stats JSONB                 -- Игровая статистика
```

### Таблица `voice_activity`
```sql
user_id TEXT PRIMARY KEY         -- Discord User ID
total_time INTEGER               -- Общее время в войсе (секунды)
last_join TIMESTAMP              -- Последнее подключение
sessions JSONB                   -- История сессий
```

---

## 🎯 Следующие шаги

После успешной настройки:

1. ✅ **Протестируй все команды**
   - !profile, !balance, !daily
   - !dice, !coinflip, !work
   - !shop, !buy, !inventory

2. ✅ **Проверь сохранение данных**
   - Перезапусти бота
   - Данные должны остаться

3. ✅ **Задеплой на сервер**
   - Запуш изменения в git
   - Railway/Render автоматически задеплоят

4. ✅ **Удали JSON файлы (опционально)**
   - После проверки что всё работает
   - Оставь как бэкап или удали

5. ✅ **Настрой бэкапы**
   - Railway/Render делают автоматические бэкапы
   - Можно настроить дополнительные

---

## 📝 Чеклист

- [ ] PostgreSQL база создана
- [ ] DATABASE_URL получен
- [ ] psycopg2-binary установлен
- [ ] DATABASE_URL добавлен в .env
- [ ] Миграция данных выполнена
- [ ] Бот запущен локально
- [ ] Команды протестированы
- [ ] Данные сохраняются
- [ ] Задеплоено на сервер
- [ ] Всё работает на сервере

---

## 🆘 Помощь

Если что-то не работает:

1. Проверь логи бота
2. Проверь логи PostgreSQL
3. Проверь переменные окружения
4. Запусти миграцию снова
5. Напиши мне - помогу разобраться!

---

**Дата:** 07.02.2026  
**Версия:** 1.0  
**Автор:** Kiro AI Assistant

🚀 Удачи с настройкой PostgreSQL!
