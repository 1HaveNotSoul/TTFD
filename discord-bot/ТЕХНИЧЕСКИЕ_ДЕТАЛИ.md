# 🔧 Технические детали миграции на Slash команды

## 📊 Статистика изменений

```
Файлов изменено: 3
Строк удалено: 820
Строк добавлено: 387
Чистое изменение: -433 строки

Коммит: c946db3
Ветка: main
Дата: 08.02.2026
```

## 🗂️ Изменённые файлы

### 1. `py/slash_commands.py`
**Изменения:** Полная замена файла  
**Было:** 11 slash команд  
**Стало:** 27 slash команд  

**Добавленные команды:**
- `/clear` - Очистка сообщений (ephemeral)
- `/help` - Динамический список команд
- `/ping` - Проверка задержки
- `/stats` - Статистика бота
- `/links` - Актуальные ссылки (переименовано из /link)
- `/buy` - Покупка предметов
- `/pay` - Перевод монет
- `/dice` - Бросок кубика
- `/coinflip` - Подбрасывание монетки
- `/ticket` - Создание тикета
- `/close` - Закрытие тикета
- `/updatecommands` - Обновление списка команд
- `/setupverification` - Настройка верификации
- `/setuptickets` - Настройка тикетов
- `/setuprankroles` - Настройка ролей рангов
- `/syncrankroles` - Синхронизация ролей

### 2. `py/game_integration.py`
**Изменения:** Переименование команды  
**Было:** `/link` (конфликт имён)  
**Стало:** `/gamelink`  

**Причина:** Конфликт с `/link` (ссылки) из `slash_commands.py`

### 3. `py/bot.py`
**Изменения:** Удаление префиксных команд, изменение порядка регистрации  

**Удалённые команды (префиксные):**
```python
@bot.command(name='ping')
@bot.command(name='help')
@bot.command(name='stats')
@bot.command(name='profile')
@bot.command(name='rank')
@bot.command(name='top')
@bot.command(name='daily')
@bot.command(name='link')
@bot.command(name='dice')
@bot.command(name='coinflip')
@bot.command(name='clear')
@bot.command(name='ticket')
@bot.command(name='close')
@bot.command(name='shop')
@bot.command(name='buy')
@bot.command(name='inventory')
@bot.command(name='balance')
@bot.command(name='pay')
@bot.command(name='work')
@bot.command(name='updatecommands')
@bot.command(name='setupverification')
@bot.command(name='setuptickets')
@bot.command(name='setuprankroles')
@bot.command(name='syncrankroles')
```

**Изменённая логика:**

#### Порядок регистрации команд
```python
# ❌ БЫЛО (неправильно):
async def on_ready():
    await slash_commands.setup_slash_commands(bot, db)
    synced = await bot.tree.sync()  # ← Слишком рано!
    
    await verification_system.setup_verification(bot)
    await tickets_system.setup_ticket_button(bot)
    
    await game_integration.setup_game_commands(bot, db, game_int)
    synced = await bot.tree.sync()  # ← Дублирование!

# ✅ СТАЛО (правильно):
async def on_ready():
    # 1. Регистрируем ВСЕ команды
    await slash_commands.setup_slash_commands(bot, db)
    await game_integration.setup_game_commands(bot, db, game_int)
    
    # 2. Синхронизируем ОДИН РАЗ с guild
    guild = discord.Object(id=config.GUILD_ID)
    bot.tree.clear_commands(guild=guild)
    synced = await bot.tree.sync(guild=guild)
    
    # 3. Настраиваем остальные системы
    await verification_system.setup_verification(bot)
    await tickets_system.setup_ticket_button(bot)
```

#### Обработка сообщений
```python
# ❌ БЫЛО (сложная логика):
@bot.event
async def on_message(message):
    if message.content.startswith('!'):
        if commands_channel.is_commands_channel(message.channel.id):
            asyncio.create_task(delete_message_after(message, 300))
            await bot.process_commands(message)
        else:
            warning_msg = await message.channel.send(...)
            await message.delete()
            asyncio.create_task(delete_message_after(warning_msg, 10))
            return
    else:
        # Начисление XP
        ...
    await bot.process_commands(message)

# ✅ СТАЛО (простая логика):
@bot.event
async def on_message(message):
    if message.author.bot:
        return
    
    bot.stats['messages_seen'] += 1
    
    # Только начисление XP
    if voice_tracking.can_earn_message_xp(message.author.id):
        xp_reward = voice_tracking.calculate_message_xp(len(message.content))
        if xp_reward > 0:
            # Начисляем XP
            ...
    
    await bot.process_commands(message)
```

#### Статус бота
```python
# ❌ БЫЛО:
discord.Activity(type=discord.ActivityType.playing, name="!help для помощи")

# ✅ СТАЛО:
discord.Activity(type=discord.ActivityType.playing, name="/help для помощи")
```

## 🔍 Ключевые улучшения

### 1. Guild Sync вместо Global Sync
```python
# ❌ Global sync (медленно, до 1 часа):
await bot.tree.sync()

# ✅ Guild sync (мгновенно):
guild = discord.Object(id=config.GUILD_ID)
await bot.tree.sync(guild=guild)
```

**Преимущества:**
- Команды появляются мгновенно (не нужно ждать)
- Работает только на конкретном сервере
- Не засоряет глобальный список команд

### 2. Clear Commands перед Sync
```python
bot.tree.clear_commands(guild=guild)
await bot.tree.sync(guild=guild)
```

**Преимущества:**
- Очищает кеш старых команд
- Предотвращает дублирование
- Гарантирует актуальный список

### 3. Ephemeral для админских команд
```python
@bot.tree.command(name="clear")
async def clear_slash(interaction: discord.Interaction, amount: int = 10):
    await interaction.response.send_message(
        "🗑️ удаляю сообщения...",
        ephemeral=True  # ← Видно только пользователю
    )
```

**Преимущества:**
- Не засоряет чат
- Конфиденциальность
- Профессиональный вид

### 4. Динамический /help
```python
@bot.tree.command(name="help")
async def help_slash(interaction: discord.Interaction):
    # Получаем список команд из bot.tree
    cmds = bot.tree.get_commands()
    
    # Группируем по категориям
    categories = {
        '👤 Профиль': ['profile', 'balance', 'rank', 'top', 'stats'],
        '🛒 Магазин': ['shop', 'inventory', 'buy', 'pay'],
        # ...
    }
```

**Преимущества:**
- Всегда актуальный список
- Автоматически обновляется при добавлении команд
- Не нужно вручную редактировать

## 🐛 Исправленные проблемы

### Проблема 1: Конфликт имён команд
**Симптом:** Только 15 команд вместо 27  
**Причина:** Две команды с именем `/link`  
**Решение:** Переименовать `/link` → `/gamelink` для игры

### Проблема 2: Двойная синхронизация
**Симптом:** Команды регистрируются дважды  
**Причина:** `sync()` вызывается до и после регистрации игровых команд  
**Решение:** Один `sync()` после регистрации ВСЕХ команд

### Проблема 3: Медленное появление команд
**Симптом:** Команды появляются через 1 час  
**Причина:** Global sync вместо guild sync  
**Решение:** Использовать guild sync

### Проблема 4: Префиксные команды не работают
**Симптом:** Бот не отвечает на `!команда`  
**Причина:** Префиксные команды удалены  
**Решение:** Это не баг, это фича! Используй `/команда`

## 📝 Конфигурация

### config.py
```python
GUILD_ID = 1466282400219922532  # ID сервера для guild sync
```

### Переменные окружения
```env
DISCORD_TOKEN=...
DATABASE_URL=...  # PostgreSQL
```

## 🧪 Тестирование

### Локальное тестирование
```bash
cd TTFD-Discord
python py/bot.py
```

### Проверка логов
```
✅ Бот успешно запущен!
✅ Slash команды зарегистрированы
✅ Интеграция с игрой настроена
✅ Синхронизировано 27 slash команд с Discord (guild sync)
```

### Проверка в Discord
```
1. Написать / в любом канале
2. Должны появиться все 27 команд
3. Протестировать основные команды
```

## 🔄 Процесс деплоя

```bash
# 1. Коммит изменений
git add .
git commit -m "feat: все команды переведены на slash формат"

# 2. Push на GitHub
git push

# 3. Railway автоматически деплоит
# Ждём 2-3 минуты

# 4. Проверяем логи Railway
# Должно быть: "Синхронизировано 27 slash команд"

# 5. Проверяем в Discord
# Пишем / и видим все команды
```

## 📚 Полезные ссылки

- [Discord.py Documentation](https://discordpy.readthedocs.io/)
- [Slash Commands Guide](https://discordpy.readthedocs.io/en/stable/interactions/api.html)
- [Railway Documentation](https://docs.railway.app/)

## 🎯 Следующие шаги

1. ✅ Миграция на slash команды - ГОТОВО
2. ⏳ Тестирование всех команд
3. ⏳ Обратная связь от пользователей
4. ⏳ Добавление новых функций

---

**Автор:** AI Assistant  
**Дата:** 08.02.2026  
**Версия:** 1.4
