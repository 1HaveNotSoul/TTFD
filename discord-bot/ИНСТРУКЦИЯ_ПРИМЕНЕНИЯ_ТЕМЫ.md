# 🎨 Инструкция по применению чёрно-красной темы

## ✅ Что уже сделано:

1. **Создан модуль `py/theme.py`** с единой цветовой схемой
2. **Обновлены команды в `bot.py`:**
   - ✅ ping
   - ✅ stats  
   - ✅ profile

## 🔧 Как применить тему к остальным командам:

### 1. Импорт в начале файла

Уже добавлено в `bot.py`:
```python
from theme import BotTheme, game_embed, profile_embed, success_embed, error_embed, warning_embed
```

### 2. Замена цветов в командах

#### Старый код:
```python
embed = discord.Embed(
    title=convert_to_font("заголовок"),
    color=discord.Color.blue()
)
```

#### Новый код (варианты):

**Для игр:**
```python
embed = game_embed(
    title=convert_to_font("заголовок"),
    description=convert_to_font("описание")  # опционально
)
```

**Для профиля:**
```python
embed = profile_embed(
    title=convert_to_font("заголовок")
)
```

**Для успеха:**
```python
embed = success_embed(
    title=convert_to_font("✅ успех!"),
    description=convert_to_font("операция выполнена")
)
```

**Для ошибки:**
```python
embed = error_embed(
    title=convert_to_font("❌ ошибка!"),
    description=convert_to_font("что-то пошло не так")
)
```

**Для предупреждения:**
```python
embed = warning_embed(
    title=convert_to_font("⚠️ внимание!"),
    description=convert_to_font("будь осторожен")
)
```

**Универсальный способ:**
```python
embed = BotTheme.create_embed(
    title=convert_to_font("заголовок"),
    description=convert_to_font("описание"),
    embed_type='game'  # или 'profile', 'shop', 'achievement', 'rank', 'ticket', 'music'
)
```

---

## 📝 Список команд для обновления:

### ✅ Уже обновлено:
- [x] ping
- [x] stats
- [x] profile

### ⏳ Нужно обновить:

#### Игровые команды:
- [ ] dice - использовать `game_embed()`
- [ ] coinflip - использовать `game_embed()`
- [ ] rank - использовать `BotTheme.create_embed(embed_type='rank')`
- [ ] top - использовать `BotTheme.create_embed(embed_type='rank')`
- [ ] daily - использовать `success_embed()` при успехе

#### Модерация:
- [ ] clear - использовать `success_embed()`

#### Тикеты:
- [ ] ticket - использовать `BotTheme.create_embed(embed_type='ticket')`
- [ ] close - использовать `warning_embed()`

#### Ссылки:
- [ ] link - использовать `BotTheme.create_embed(embed_type='info')`

---

## 🎨 Цветовая палитра:

```
Чёрно-красный градиент (8 оттенков):

1. #0d0d0d - Чёрный
2. #1a0000 - Очень тёмный красный
3. #330000 - Тёмный красный
4. #660000 - Средне-тёмный красный
5. #8b0000 - Кроваво-красный
6. #cc0000 - Красный
7. #ff0000 - Ярко-красный
8. #ff4444 - Светло-красный
```

### Применение по типам:

| Тип | Цвет | Hex | Использование |
|-----|------|-----|---------------|
| **success** | Зелёный | #00ff00 | Успешные операции |
| **error** | Красный | #ff0000 | Ошибки |
| **warning** | Оранжево-красный | #ff6600 | Предупреждения |
| **info** | Светло-красный | #ff4444 | Информация |
| **game** | Кроваво-красный | #8b0000 | Игры |
| **profile** | Светло-красный | #ff4444 | Профили |
| **shop** | Ярко-красный | #ff0000 | Магазин |
| **achievement** | Кроваво-красный | #8b0000 | Достижения |
| **rank** | Кроваво-красный | #8b0000 | Ранги |
| **ticket** | Очень тёмный красный | #1a0000 | Тикеты |
| **music** | Светло-красный | #ff4444 | Музыка |

---

## 🔄 Автоматическое применение:

### Вариант 1: Ручная замена

Открой `bot.py` и замени все `discord.Embed(color=discord.Color.blue())` на соответствующие функции из `theme.py`.

### Вариант 2: Поиск и замена

1. Найди все `discord.Embed(`
2. Определи тип embed (игра, профиль, ошибка и т.д.)
3. Замени на соответствующую функцию

---

## 📋 Примеры замены:

### Пример 1: Команда dice

**Было:**
```python
embed = discord.Embed(
    title=convert_to_font("🎲 Бросок кубика"),
    description=convert_to_font(f"Выпало: {dice_emoji[result-1]} {result}"),
    color=discord.Color.blue()
)
```

**Стало:**
```python
embed = game_embed(
    title=convert_to_font("🎲 бросок кубика"),
    description=convert_to_font(f"выпало: {dice_emoji[result-1]} {result}")
)
```

### Пример 2: Команда rank

**Было:**
```python
embed = discord.Embed(
    title=convert_to_font(f"🏆 Твой ранг: {rank_info['name']}"),
    color=discord.Color.gold()
)
```

**Стало:**
```python
embed = BotTheme.create_embed(
    title=convert_to_font(f"🏆 твой ранг: {rank_info['name']}"),
    embed_type='rank'
)
```

### Пример 3: Команда daily (успех)

**Было:**
```python
embed = discord.Embed(
    title=convert_to_font("🎁 Ежедневная награда получена!"),
    color=discord.Color.green()
)
```

**Стало:**
```python
embed = success_embed(
    title=convert_to_font("🎁 ежедневная награда получена!")
)
```

### Пример 4: Ошибка

**Было:**
```python
embed = discord.Embed(
    title=convert_to_font("❌ Ошибка"),
    description=convert_to_font("Что-то пошло не так"),
    color=discord.Color.red()
)
```

**Стало:**
```python
embed = error_embed(
    title=convert_to_font("❌ ошибка"),
    description=convert_to_font("что-то пошло не так")
)
```

---

## 🚀 Быстрый старт:

1. **Убедись, что `theme.py` импортирован:**
   ```python
   from theme import BotTheme, game_embed, profile_embed, success_embed, error_embed, warning_embed
   ```

2. **Замени все embed в командах:**
   - Игры → `game_embed()`
   - Профиль → `profile_embed()`
   - Успех → `success_embed()`
   - Ошибка → `error_embed()`
   - Предупреждение → `warning_embed()`

3. **Перезапусти бота**

4. **Проверь команды:**
   ```
   !ping
   !stats
   !profile
   !dice
   !coinflip
   !rank
   !top
   !daily
   ```

---

## 🎨 Дополнительные возможности:

### Градиентные цвета

Если хочешь использовать разные оттенки градиента:

```python
# Получить цвет из градиента (0-7)
color = BotTheme.get_gradient_color(3)  # Средне-тёмный красный

embed = discord.Embed(
    title=convert_to_font("заголовок"),
    color=color
)
```

### Кастомный цвет

```python
embed = BotTheme.create_embed(
    title=convert_to_font("заголовок"),
    color=0x8b0000  # Кроваво-красный
)
```

### Эмодзи из темы

```python
# Использовать эмодзи из темы
emoji = BotTheme.EMOJI['fire']  # 🔥
emoji = BotTheme.EMOJI['crown']  # 👑
emoji = BotTheme.EMOJI['game']  # 🎮
```

---

## ✅ Проверка применения темы:

После обновления всех команд, проверь:

1. **Все embed имеют чёрно-красные оттенки** ✓
2. **Нет синих/зелёных/золотых цветов** (кроме success) ✓
3. **Единый стиль во всех сообщениях** ✓
4. **Текст в lowercase** (для шрифта) ✓

---

## 📞 Если нужна помощь:

1. Проверь, что `theme.py` находится в папке `py/`
2. Проверь импорт в начале `bot.py`
3. Убедись, что используешь правильные функции
4. Перезапусти бота после изменений

---

**Дата:** 02.02.2026  
**Версия:** 1.0  
**Автор:** Kiro AI Assistant
