# 📝 Как добавить новую команду в бота

## Шаг 1: Создать команду в bot.py

Добавьте новую команду в файл `py/bot.py`:

```python
@bot.command(name='название_команды')
async def название_команды(ctx):
    """Описание команды"""
    # Ваш код здесь
    await ctx.send(convert_to_font("Текст ответа"))
```

## Шаг 2: Добавить команду в список

Откройте файл `py/commands_manager.py` и добавьте команду в соответствующую категорию в словаре `COMMANDS_LIST`:

```python
COMMANDS_LIST = {
    '🎮 мини-игры': [
        ('!dice', 'бросить кубик (1-6)'),
        ('!coinflip', 'подбросить монетку'),
        ('!новая_команда', 'описание новой команды'),  # ← Добавьте сюда
    ],
}
```

### Доступные категории:

- `📌 основные` - базовые команды бота
- `👤 профиль и игра` - команды профиля и прогресса
- `🎮 мини-игры` - игровые команды
- `💬 поддержка` - тикеты и помощь
- `🛡️ модерация` - команды модерации
- `⚙️ администрирование` - команды для администраторов

## Шаг 3: Обновить список команд

После добавления команды список автоматически обновится при перезапуске бота.

Для ручного обновления используйте команду:
```
!updatecommands
```

## Пример: Добавление команды !roll

### 1. В bot.py:
```python
@bot.command(name='roll')
async def roll(ctx, sides: int = 20):
    """Бросить кубик с N гранями"""
    result = random.randint(1, sides)
    await ctx.send(convert_to_font(f"🎲 Выпало: {result} из {sides}"))
```

### 2. В commands_manager.py:
```python
'🎮 мини-игры': [
    ('!dice', 'бросить кубик (1-6)'),
    ('!coinflip', 'подбросить монетку'),
    ('!roll', 'бросить кубик с N гранями'),  # ← Новая команда
],
```

### 3. Обновить список:
```
!updatecommands
```

## ⚠️ Важно!

- Всегда используйте `convert_to_font()` для текста в сообщениях
- Добавляйте команду в правильную категорию
- Используйте строчные буквы в названиях команд
- Описание должно быть кратким и понятным

## 🔄 Автоматическое обновление

Список команд автоматически обновляется:
- При запуске бота
- При использовании команды `!updatecommands`

Канал для списка команд: `1466295322002067607`
