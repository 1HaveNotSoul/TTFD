# 📚 Примеры добавления команд

## 🎯 Простая команда

### Код в bot.py:
```python
@bot.command(name='hello')
async def hello(ctx):
    """Поприветствовать пользователя"""
    await ctx.send(convert_to_font(f"привет, {ctx.author.name}!"))
```

### Добавить в commands_manager.py:
```python
'📌 основные': [
    ('!hello', 'поприветствовать пользователя'),
],
```

---

## 🎲 Команда с параметрами

### Код в bot.py:
```python
@bot.command(name='roll')
async def roll(ctx, sides: int = 20):
    """Бросить кубик с N гранями"""
    if sides < 2 or sides > 100:
        await ctx.send(convert_to_font("❌ укажи число от 2 до 100!"))
        return
    
    result = random.randint(1, sides)
    await ctx.send(convert_to_font(f"🎲 выпало: {result} из {sides}"))
```

### Добавить в commands_manager.py:
```python
'🎮 мини-игры': [
    ('!roll [число]', 'бросить кубик с N гранями'),
],
```

---

## 💰 Команда с базой данных

### Код в bot.py:
```python
@bot.command(name='balance')
async def balance(ctx):
    """Проверить баланс"""
    user = db.get_user(str(ctx.author.id))
    
    if not user:
        await ctx.send(convert_to_font("❌ ты не зарегистрирован!"))
        return
    
    coins = user.get('coins', 0)
    
    embed = discord.Embed(
        title=convert_to_font("💰 твой баланс"),
        description=convert_to_font(f"монеты: {coins}"),
        color=discord.Color.gold()
    )
    await ctx.send(embed=embed)
```

### Добавить в commands_manager.py:
```python
'👤 профиль и игра': [
    ('!balance', 'проверить баланс'),
],
```

---

## 🛡️ Команда для модераторов

### Код в bot.py:
```python
@bot.command(name='kick')
@commands.has_permissions(kick_members=True)
async def kick(ctx, member: discord.Member, *, reason: str = "не указана"):
    """Кикнуть пользователя"""
    try:
        await member.kick(reason=reason)
        await ctx.send(convert_to_font(f"✅ {member.name} был кикнут. причина: {reason}"))
    except Exception as e:
        await ctx.send(convert_to_font(f"❌ ошибка: {e}"))

@kick.error
async def kick_error(ctx, error):
    if isinstance(error, commands.MissingPermissions):
        await ctx.send(convert_to_font("❌ у тебя нет прав!"))
```

### Добавить в commands_manager.py:
```python
'🛡️ модерация': [
    ('!kick', 'кикнуть пользователя'),
],
```

---

## 🎨 Команда с Embed

### Код в bot.py:
```python
@bot.command(name='info')
async def info(ctx):
    """Информация о сервере"""
    guild = ctx.guild
    
    embed = discord.Embed(
        title=convert_to_font(f"информация о {guild.name}"),
        color=discord.Color.blue(),
        timestamp=datetime.now()
    )
    embed.set_thumbnail(url=guild.icon.url if guild.icon else None)
    embed.add_field(
        name=convert_to_font("👥 участников"),
        value=convert_to_font(str(guild.member_count)),
        inline=True
    )
    embed.add_field(
        name=convert_to_font("📅 создан"),
        value=convert_to_font(guild.created_at.strftime("%d.%m.%Y")),
        inline=True
    )
    
    await ctx.send(embed=embed)
```

### Добавить в commands_manager.py:
```python
'📌 основные': [
    ('!info', 'информация о сервере'),
],
```

---

## 🌐 Команда с API запросом

### Код в bot.py:
```python
@bot.command(name='weather')
async def weather(ctx, *, city: str):
    """Погода в городе"""
    async with aiohttp.ClientSession() as session:
        try:
            url = f"https://api.openweathermap.org/data/2.5/weather?q={city}&appid=YOUR_API_KEY&units=metric&lang=ru"
            async with session.get(url) as response:
                if response.status == 200:
                    data = await response.json()
                    temp = data['main']['temp']
                    desc = data['weather'][0]['description']
                    
                    embed = discord.Embed(
                        title=convert_to_font(f"🌤️ погода в {city}"),
                        description=convert_to_font(f"температура: {temp}°C\n{desc}"),
                        color=discord.Color.blue()
                    )
                    await ctx.send(embed=embed)
                else:
                    await ctx.send(convert_to_font("❌ город не найден!"))
        except Exception as e:
            await ctx.send(convert_to_font(f"❌ ошибка: {e}"))
```

### Добавить в commands_manager.py:
```python
'📌 основные': [
    ('!weather', 'погода в городе'),
],
```

---

## ⚠️ Важные правила

1. **Всегда используйте `convert_to_font()`** для всего текста
2. **Используйте строчные буквы** в русском тексте
3. **Добавляйте команду в правильную категорию**
4. **Обновляйте список** командой `!updatecommands`

---

## 🔄 После добавления команды

1. Сохраните изменения в `bot.py`
2. Добавьте команду в `commands_manager.py`
3. Перезапустите бота или используйте `!updatecommands`
4. Проверьте канал команд (ID: 1466295322002067607)

---

## 📝 Шаблон для копирования

```python
@bot.command(name='название')
async def название(ctx):
    """Описание команды"""
    # Ваш код
    await ctx.send(convert_to_font("текст"))
```

```python
'категория': [
    ('!название', 'описание'),
],
```
