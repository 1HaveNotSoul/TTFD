# 🎨 Как использовать шрифт Small Capital в боте

## 📝 Описание

Бот использует специальный шрифт **Small Capital** для красивого отображения текста в Discord.

## ✅ Что сделано

1. ✅ Создан правильный `font_converter.py` с функциями конвертации
2. ✅ Функция автоматически импортирована в `bot.py`
3. ✅ Все команды бота используют шрифт

## 🎯 Примеры конвертации

```
Обычный текст:    Привет, мир!
С шрифтом:        ᴨᴩиʙᴇᴛ, ʍиᴩ!

Обычный текст:    Hello, World!
С шрифтом:        ʜᴇʟʟᴏ, ᴡᴏʀʟᴅ!

Обычный текст:    Профиль
С шрифтом:        ᴨᴩоɸиᴧь

Обычный текст:    Модерация
С шрифтом:        ʍодᴇᴩᴀция
```

## 🔧 Как использовать в коде

### Вариант 1: Обернуть текст в функцию

```python
from font_converter import convert_to_font

# Было:
embed = discord.Embed(
    title="Привет!",
    description="Это сообщение",
    color=discord.Color.blue()
)

# Стало:
embed = discord.Embed(
    title=convert_to_font("Привет!"),
    description=convert_to_font("Это сообщение"),
    color=discord.Color.blue()
)
```

### Вариант 2: Для обычных сообщений

```python
# Было:
await ctx.send("Привет!")

# Стало:
await ctx.send(convert_to_font("Привет!"))
```

### Вариант 3: Для полей embed

```python
# Было:
embed.add_field(name="Ранг", value="Новичок", inline=True)

# Стало:
embed.add_field(
    name=convert_to_font("Ранг"), 
    value=convert_to_font("Новичок"), 
    inline=True
)
```

## 📋 Пример команды с шрифтом

```python
@bot.command(name='profile')
async def profile(ctx, member: discord.Member = None):
    member = member or ctx.author
    user = db.get_user(str(member.id))
    
    embed = discord.Embed(
        title=convert_to_font(f"👤 Профиль {member.display_name}"),
        color=discord.Color.blue()
    )
    embed.set_thumbnail(url=member.display_avatar.url)
    embed.add_field(
        name=convert_to_font("🏆 Ранг"), 
        value=convert_to_font("Новичок"), 
        inline=True
    )
    embed.add_field(
        name=convert_to_font("⭐ XP"), 
        value=convert_to_font(str(user['xp'])), 
        inline=True
    )
    
    await ctx.send(embed=embed)
```

## ⚠️ Что НЕ нужно конвертировать

- ❌ Числа: `100`, `500`, `1000`
- ❌ Эмодзи: `🎮`, `🏆`, `💰` (они сохраняются автоматически)
- ❌ Специальные символы: `**`, `*`, `` ` ``, `\n`
- ❌ URL: `https://ttfd.onrender.com/`
- ❌ Имена пользователей в переменных: `{ctx.author.name}`
- ❌ Переменные с числами: `{latency}`, `{amount}`

## ✅ Что нужно конвертировать

- ✅ Заголовки embed'ов
- ✅ Описания embed'ов
- ✅ Названия полей
- ✅ Значения полей (если это текст)
- ✅ Обычные текстовые сообщения
- ✅ Сообщения об ошибках
- ✅ Footer текст

## 📚 Особенности шрифта

### Английские буквы
Все английские буквы конвертируются:
```
abcdefghijklmnopqrstuvwxyz
ᴀʙᴄᴅᴇꜰɢʜɪᴊᴋʟᴍɴᴏᴘǫʀsᴛᴜᴠᴡxʏᴢ
```

### Русские буквы
Большинство русских букв конвертируются:
```
абвгеёжзклмпрстуфхшэ
ᴀʙʙᴦᴇᴇᴊᴢᴋᴧʍᴨᴩᴄᴛʏɸxᴡᴇ
```

Некоторые буквы остаются без изменений:
```
и, й, о, д, н, ц, ч, щ, ъ, ы, ь, ю, я
```

## 🧪 Тестирование

Проверь конвертер:
```bash
python font_converter.py
```

Или используй тестовый скрипт:
```bash
python test_bot_font.py
```

## 🌐 Источник шрифта

Шрифт основан на Unicode Small Capital Letters.  
Онлайн генератор: https://textgenerator.ru/font/small-capital

## 📖 Функции API

### `convert_to_font(text)`
Конвертирует обычный текст в Small Capital шрифт.

**Параметры:**
- `text` (str) - обычный текст

**Возвращает:**
- `str` - текст в Small Capital шрифте

**Пример:**
```python
>>> from font_converter import convert_to_font
>>> convert_to_font("Привет")
'ᴨᴩиʙᴇᴛ'
>>> convert_to_font("Hello")
'ʜᴇʟʟᴏ'
```

### `convert_from_font(text)`
Конвертирует текст из Small Capital обратно в обычный (частично).

**Параметры:**
- `text` (str) - текст в Small Capital шрифте

**Возвращает:**
- `str` - обычный текст

**Примечание:** Обратная конвертация может быть неточной.

## 💡 Советы

1. **Используйте для заголовков** - шрифт делает их более заметными
2. **Не злоупотребляйте** - слишком много текста в шрифте трудно читать
3. **Комбинируйте с эмодзи** - они отлично сочетаются
4. **Тестируйте** - убедитесь, что текст читается правильно

## ✅ Готово!

Теперь все сообщения бота отображаются в красивом шрифте Small Capital!

---

**Обновлено:** 31.01.2026  
**Статус:** ✅ Работает корректно
