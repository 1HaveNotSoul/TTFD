# 💻 Примеры кода для новых функций

## 🎮 1. Игра Slots (Игровой автомат)

```python
# В bot.py

import random

SLOT_SYMBOLS = ['🍒', '🍋', '🍊', '🍇', '💎', '7️⃣']
SLOT_MULTIPLIERS = {
    '💎💎💎': 50,  # Джекпот!
    '7️⃣7️⃣7️⃣': 25,
    '🍇🍇🍇': 10,
    '🍊🍊🍊': 5,
    '🍋🍋🍋': 3,
    '🍒🍒🍒': 2,
}

@bot.command(name='slots')
async def slots(ctx, bet: int = 10):
    """Игровой автомат"""
    user = db.get_user(str(ctx.author.id))
    
    if not user:
        await ctx.send(convert_to_font("❌ ты не зарегистрирован!"))
        return
    
    # Проверка ставки
    if bet < 10 or bet > 1000:
        await ctx.send(convert_to_font("❌ ставка должна быть от 10 до 1000 монет!"))
        return
    
    if user['coins'] < bet:
        await ctx.send(convert_to_font(f"❌ недостаточно монет! у тебя: {user['coins']}"))
        return
    
    # Вычитаем ставку
    user['coins'] -= bet
    
    # Крутим барабаны
    result = [random.choice(SLOT_SYMBOLS) for _ in range(3)]
    result_str = ''.join(result)
    
    # Проверяем выигрыш
    win_amount = 0
    multiplier = 0
    
    if result_str in SLOT_MULTIPLIERS:
        multiplier = SLOT_MULTIPLIERS[result_str]
        win_amount = bet * multiplier
        user['coins'] += win_amount
        user['games_won'] = user.get('games_won', 0) + 1
    
    # Обновляем статистику
    user['games_played'] = user.get('games_played', 0) + 1
    db.save_user(str(ctx.author.id), user)
    
    # Создаём embed
    embed = discord.Embed(
        title=convert_to_font("🎰 игровой автомат"),
        color=discord.Color.gold() if win_amount > 0 else discord.Color.red()
    )
    
    # Анимация (3 сообщения)
    spinning_msg = await ctx.send(convert_to_font("🎰 крутим барабаны..."))
    await asyncio.sleep(1)
    await spinning_msg.edit(content=f"🎰 | {result[0]} | ? | ? |")
    await asyncio.sleep(0.5)
    await spinning_msg.edit(content=f"🎰 | {result[0]} | {result[1]} | ? |")
    await asyncio.sleep(0.5)
    await spinning_msg.edit(content=f"🎰 | {result[0]} | {result[1]} | {result[2]} |")
    await asyncio.sleep(0.5)
    
    # Результат
    embed.add_field(
        name=convert_to_font("результат"),
        value=f"| {result[0]} | {result[1]} | {result[2]} |",
        inline=False
    )
    
    embed.add_field(
        name=convert_to_font("ставка"),
        value=convert_to_font(f"{bet} монет"),
        inline=True
    )
    
    if win_amount > 0:
        embed.add_field(
            name=convert_to_font("🎉 выигрыш!"),
            value=convert_to_font(f"+{win_amount} монет (x{multiplier})"),
            inline=True
        )
        embed.description = convert_to_font("поздравляем с выигрышем!")
    else:
        embed.add_field(
            name=convert_to_font("😔 проигрыш"),
            value=convert_to_font(f"-{bet} монет"),
            inline=True
        )
        embed.description = convert_to_font("попробуй ещё раз!")
    
    embed.add_field(
        name=convert_to_font("баланс"),
        value=convert_to_font(f"{user['coins']} монет"),
        inline=False
    )
    
    await spinning_msg.delete()
    await ctx.send(embed=embed)
```

---

## ✊ 2. Игра Rock-Paper-Scissors (Камень-ножницы-бумага)

```python
# В bot.py

@bot.command(name='rps')
async def rps(ctx, choice: str = None, opponent: discord.Member = None):
    """Камень-ножницы-бумага"""
    user = db.get_user(str(ctx.author.id))
    
    if not user:
        await ctx.send(convert_to_font("❌ ты не зарегистрирован!"))
        return
    
    # Проверка выбора
    choices = {
        'камень': '🪨',
        'ножницы': '✂️',
        'бумага': '📄',
        'rock': '🪨',
        'scissors': '✂️',
        'paper': '📄'
    }
    
    if not choice or choice.lower() not in choices:
        await ctx.send(convert_to_font("❌ выбери: камень, ножницы или бумага"))
        return
    
    user_choice = choice.lower()
    user_emoji = choices[user_choice]
    
    # Игра с ботом или игроком
    if opponent and opponent != ctx.author and not opponent.bot:
        # PvP режим (можно расширить с кнопками)
        await ctx.send(convert_to_font("⏳ pvp режим в разработке! пока играй с ботом"))
        return
    
    # Игра с ботом
    bot_choices = ['камень', 'ножницы', 'бумага']
    bot_choice = random.choice(bot_choices)
    bot_emoji = choices[bot_choice]
    
    # Определяем победителя
    win_conditions = {
        'камень': 'ножницы',
        'ножницы': 'бумага',
        'бумага': 'камень',
        'rock': 'scissors',
        'scissors': 'paper',
        'paper': 'rock'
    }
    
    if user_choice in ['rock', 'scissors', 'paper']:
        user_choice_ru = {'rock': 'камень', 'scissors': 'ножницы', 'paper': 'бумага'}[user_choice]
    else:
        user_choice_ru = user_choice
    
    result = None
    xp_reward = 0
    
    if user_choice_ru == bot_choice:
        result = "ничья"
        xp_reward = 10
    elif win_conditions[user_choice_ru] == bot_choice:
        result = "победа"
        xp_reward = 30
        user['games_won'] = user.get('games_won', 0) + 1
    else:
        result = "поражение"
        xp_reward = 5
    
    # Обновляем статистику
    user['games_played'] = user.get('games_played', 0) + 1
    user['xp'] = user.get('xp', 0) + xp_reward
    db.save_user(str(ctx.author.id), user)
    
    # Создаём embed
    color_map = {
        "победа": discord.Color.green(),
        "поражение": discord.Color.red(),
        "ничья": discord.Color.gold()
    }
    
    embed = discord.Embed(
        title=convert_to_font("✊ камень-ножницы-бумага"),
        color=color_map[result]
    )
    
    embed.add_field(
        name=convert_to_font("твой выбор"),
        value=f"{user_emoji} {convert_to_font(user_choice_ru)}",
        inline=True
    )
    
    embed.add_field(
        name=convert_to_font("выбор бота"),
        value=f"{bot_emoji} {convert_to_font(bot_choice)}",
        inline=True
    )
    
    result_emoji = {"победа": "🎉", "поражение": "😔", "ничья": "🤝"}
    embed.add_field(
        name=convert_to_font("результат"),
        value=f"{result_emoji[result]} {convert_to_font(result)}",
        inline=False
    )
    
    embed.add_field(
        name=convert_to_font("награда"),
        value=convert_to_font(f"+{xp_reward} xp"),
        inline=True
    )
    
    await ctx.send(embed=embed)
```

---

## 💰 3. Магазин (Shop System)

```python
# В новом файле: py/shop_system.py

from font_converter import convert_to_font
import discord

# Предметы магазина
SHOP_ITEMS = {
    'roles': [
        {
            'id': 'vip_role',
            'name': 'VIP роль',
            'description': 'Особая роль с крутым цветом',
            'price': 5000,
            'role_id': 1234567890,  # ID роли на сервере
            'emoji': '👑'
        },
        {
            'id': 'premium_role',
            'name': 'Premium роль',
            'description': 'Премиум роль с привилегиями',
            'price': 10000,
            'role_id': 1234567891,
            'emoji': '💎'
        },
    ],
    'boosts': [
        {
            'id': 'xp_boost_1h',
            'name': 'Буст XP (1 час)',
            'description': 'x2 XP на 1 час',
            'price': 500,
            'duration': 3600,  # секунды
            'multiplier': 2,
            'emoji': '⚡'
        },
        {
            'id': 'xp_boost_24h',
            'name': 'Буст XP (24 часа)',
            'description': 'x2 XP на 24 часа',
            'price': 3000,
            'duration': 86400,
            'multiplier': 2,
            'emoji': '🚀'
        },
    ],
    'cosmetics': [
        {
            'id': 'custom_emoji',
            'name': 'Кастомный эмодзи',
            'description': 'Добавь свой эмодзи на сервер',
            'price': 2000,
            'emoji': '😎'
        },
        {
            'id': 'profile_bg',
            'name': 'Фон профиля',
            'description': 'Кастомный фон для !profile',
            'price': 1500,
            'emoji': '🎨'
        },
    ]
}

def get_shop_embed(category='all'):
    """Создать embed магазина"""
    embed = discord.Embed(
        title=convert_to_font("🛒 магазин"),
        description=convert_to_font("купи предметы за монеты!"),
        color=discord.Color.blue()
    )
    
    if category == 'all' or category == 'roles':
        roles_text = []
        for item in SHOP_ITEMS['roles']:
            roles_text.append(
                f"{item['emoji']} **{convert_to_font(item['name'])}** - {convert_to_font(str(item['price']))} монет\n"
                f"   {convert_to_font(item['description'])}"
            )
        if roles_text:
            embed.add_field(
                name=convert_to_font("👑 роли"),
                value='\n\n'.join(roles_text),
                inline=False
            )
    
    if category == 'all' or category == 'boosts':
        boosts_text = []
        for item in SHOP_ITEMS['boosts']:
            boosts_text.append(
                f"{item['emoji']} **{convert_to_font(item['name'])}** - {convert_to_font(str(item['price']))} монет\n"
                f"   {convert_to_font(item['description'])}"
            )
        if boosts_text:
            embed.add_field(
                name=convert_to_font("⚡ бусты"),
                value='\n\n'.join(boosts_text),
                inline=False
            )
    
    if category == 'all' or category == 'cosmetics':
        cosmetics_text = []
        for item in SHOP_ITEMS['cosmetics']:
            cosmetics_text.append(
                f"{item['emoji']} **{convert_to_font(item['name'])}** - {convert_to_font(str(item['price']))} монет\n"
                f"   {convert_to_font(item['description'])}"
            )
        if cosmetics_text:
            embed.add_field(
                name=convert_to_font("🎨 косметика"),
                value='\n\n'.join(cosmetics_text),
                inline=False
            )
    
    embed.set_footer(text=convert_to_font("используй !buy [id] для покупки"))
    
    return embed

def find_item(item_id):
    """Найти предмет по ID"""
    for category in SHOP_ITEMS.values():
        for item in category:
            if item['id'] == item_id:
                return item
    return None

async def buy_item(ctx, bot, db, item_id):
    """Купить предмет"""
    user = db.get_user(str(ctx.author.id))
    
    if not user:
        return False, convert_to_font("❌ ты не зарегистрирован!")
    
    item = find_item(item_id)
    
    if not item:
        return False, convert_to_font("❌ предмет не найден!")
    
    # Проверка баланса
    if user['coins'] < item['price']:
        return False, convert_to_font(f"❌ недостаточно монет! нужно: {item['price']}, у тебя: {user['coins']}")
    
    # Проверка инвентаря (если уже есть)
    if 'inventory' not in user:
        user['inventory'] = []
    
    if item_id in user['inventory']:
        return False, convert_to_font("❌ у тебя уже есть этот предмет!")
    
    # Покупка
    user['coins'] -= item['price']
    user['inventory'].append(item_id)
    
    # Особая обработка для ролей
    if 'role_id' in item:
        try:
            role = ctx.guild.get_role(item['role_id'])
            if role:
                await ctx.author.add_roles(role)
        except Exception as e:
            print(f"Ошибка выдачи роли: {e}")
    
    db.save_user(str(ctx.author.id), user)
    
    return True, convert_to_font(f"✅ куплено: {item['name']}! осталось монет: {user['coins']}")


# В bot.py добавить команды:

@bot.command(name='shop')
async def shop(ctx, category: str = 'all'):
    """Магазин предметов"""
    embed = shop_system.get_shop_embed(category)
    await ctx.send(embed=embed)

@bot.command(name='buy')
async def buy(ctx, item_id: str):
    """Купить предмет"""
    success, message = await shop_system.buy_item(ctx, bot, db, item_id)
    
    if success:
        embed = discord.Embed(
            title=convert_to_font("✅ покупка успешна!"),
            description=message,
            color=discord.Color.green()
        )
    else:
        embed = discord.Embed(
            title=convert_to_font("❌ ошибка покупки"),
            description=message,
            color=discord.Color.red()
        )
    
    await ctx.send(embed=embed)

@bot.command(name='inventory')
async def inventory(ctx):
    """Инвентарь пользователя"""
    user = db.get_user(str(ctx.author.id))
    
    if not user:
        await ctx.send(convert_to_font("❌ ты не зарегистрирован!"))
        return
    
    inventory = user.get('inventory', [])
    
    embed = discord.Embed(
        title=convert_to_font("🎒 твой инвентарь"),
        color=discord.Color.blue()
    )
    
    if not inventory:
        embed.description = convert_to_font("твой инвентарь пуст. посети !shop")
    else:
        items_text = []
        for item_id in inventory:
            item = shop_system.find_item(item_id)
            if item:
                items_text.append(f"{item['emoji']} {convert_to_font(item['name'])}")
        
        embed.description = '\n'.join(items_text)
    
    embed.add_field(
        name=convert_to_font("💰 монеты"),
        value=convert_to_font(str(user['coins'])),
        inline=True
    )
    
    await ctx.send(embed=embed)
```

---

## 🏆 4. Система достижений

```python
# В новом файле: py/achievements_system.py

from font_converter import convert_to_font
import discord
from datetime import datetime

# Список достижений
ACHIEVEMENTS = [
    {
        'id': 'first_steps',
        'name': 'Первые шаги',
        'description': 'Получи 2 ранг',
        'emoji': '👣',
        'reward_xp': 100,
        'reward_coins': 50,
        'condition': lambda user: user['rank_id'] >= 2
    },
    {
        'id': 'gamer',
        'name': 'Игроман',
        'description': 'Сыграй 100 игр',
        'emoji': '🎮',
        'reward_xp': 500,
        'reward_coins': 250,
        'condition': lambda user: user.get('games_played', 0) >= 100
    },
    {
        'id': 'lucky',
        'name': 'Везунчик',
        'description': 'Выиграй 10 раз подряд',
        'emoji': '🍀',
        'reward_xp': 1000,
        'reward_coins': 500,
        'condition': lambda user: user.get('win_streak', 0) >= 10
    },
    {
        'id': 'millionaire',
        'name': 'Миллионер',
        'description': 'Накопи 10000 монет',
        'emoji': '💰',
        'reward_xp': 2000,
        'reward_coins': 1000,
        'condition': lambda user: user.get('coins', 0) >= 10000
    },
    {
        'id': 'legend',
        'name': 'Легенда',
        'description': 'Достигни 20 ранга',
        'emoji': '👑',
        'reward_xp': 5000,
        'reward_coins': 5000,
        'condition': lambda user: user['rank_id'] >= 20
    },
    {
        'id': 'loyal',
        'name': 'Верный друг',
        'description': '30 дней streak',
        'emoji': '🔥',
        'reward_xp': 3000,
        'reward_coins': 2000,
        'condition': lambda user: user.get('daily_streak', 0) >= 30
    },
]

def check_achievements(user, db):
    """Проверить и выдать новые достижения"""
    if 'achievements' not in user:
        user['achievements'] = []
    
    new_achievements = []
    
    for achievement in ACHIEVEMENTS:
        # Если достижение уже получено - пропускаем
        if achievement['id'] in user['achievements']:
            continue
        
        # Проверяем условие
        if achievement['condition'](user):
            # Выдаём достижение
            user['achievements'].append(achievement['id'])
            user['xp'] = user.get('xp', 0) + achievement['reward_xp']
            user['coins'] = user.get('coins', 0) + achievement['reward_coins']
            new_achievements.append(achievement)
    
    if new_achievements:
        db.save_user(user['id'], user)
    
    return new_achievements

def get_achievements_embed(user):
    """Создать embed со списком достижений"""
    embed = discord.Embed(
        title=convert_to_font("🏆 достижения"),
        color=discord.Color.gold()
    )
    
    user_achievements = user.get('achievements', [])
    total = len(ACHIEVEMENTS)
    unlocked = len(user_achievements)
    
    embed.description = convert_to_font(f"получено: {unlocked}/{total}")
    
    for achievement in ACHIEVEMENTS:
        is_unlocked = achievement['id'] in user_achievements
        
        if is_unlocked:
            status = "✅"
            name = achievement['name']
        else:
            status = "🔒"
            name = "???"
        
        value = f"{status} {convert_to_font(achievement['description'])}\n"
        value += convert_to_font(f"награда: {achievement['reward_xp']} xp, {achievement['reward_coins']} монет")
        
        embed.add_field(
            name=f"{achievement['emoji']} {convert_to_font(name)}",
            value=value,
            inline=False
        )
    
    return embed


# В bot.py добавить:

@bot.command(name='achievements')
async def achievements(ctx):
    """Список достижений"""
    user = db.get_user(str(ctx.author.id))
    
    if not user:
        await ctx.send(convert_to_font("❌ ты не зарегистрирован!"))
        return
    
    embed = achievements_system.get_achievements_embed(user)
    await ctx.send(embed=embed)

# В конце каждой игровой команды добавить проверку:
# new_achievements = achievements_system.check_achievements(user, db)
# if new_achievements:
#     for ach in new_achievements:
#         await ctx.send(f"🎉 {convert_to_font('новое достижение!')}\n"
#                       f"{ach['emoji']} **{convert_to_font(ach['name'])}**\n"
#                       f"{convert_to_font(ach['description'])}")
```

---

## 💼 5. Команда Work (Работа)

```python
# В bot.py

import random
from datetime import datetime, timedelta

WORK_JOBS = [
    ("программист", "написал код для сайта", 150, 250),
    ("дизайнер", "создал крутой дизайн", 120, 200),
    ("модератор", "почистил чат от спама", 80, 150),
    ("стример", "провёл стрим на 100 зрителей", 200, 300),
    ("музыкант", "записал новый трек", 100, 180),
    ("художник", "нарисовал арт", 90, 170),
    ("писатель", "написал статью", 70, 140),
    ("геймер", "выиграл турнир", 180, 280),
]

@bot.command(name='work')
async def work(ctx):
    """Поработать и заработать монеты"""
    user = db.get_user(str(ctx.author.id))
    
    if not user:
        await ctx.send(convert_to_font("❌ ты не зарегистрирован!"))
        return
    
    # Проверка кулдауна (1 час)
    if 'last_work' in user and user['last_work']:
        last_work = datetime.fromisoformat(user['last_work'])
        time_diff = (datetime.now() - last_work).total_seconds()
        
        if time_diff < 3600:  # 1 час
            time_left = 3600 - time_diff
            hours = int(time_left // 3600)
            minutes = int((time_left % 3600) // 60)
            
            await ctx.send(convert_to_font(f"⏰ ты уже работал! приходи через {hours}ч {minutes}м"))
            return
    
    # Выбираем случайную работу
    job_name, job_desc, min_reward, max_reward = random.choice(WORK_JOBS)
    reward = random.randint(min_reward, max_reward)
    
    # Бонус за ранг (1% за ранг)
    rank_bonus = int(reward * (user['rank_id'] / 100))
    total_reward = reward + rank_bonus
    
    # Добавляем монеты
    user['coins'] = user.get('coins', 0) + total_reward
    user['last_work'] = datetime.now().isoformat()
    
    db.save_user(str(ctx.author.id), user)
    
    # Создаём embed
    embed = discord.Embed(
        title=convert_to_font("💼 работа"),
        description=convert_to_font(f"ты поработал как {job_name}"),
        color=discord.Color.green()
    )
    
    embed.add_field(
        name=convert_to_font("что сделал"),
        value=convert_to_font(job_desc),
        inline=False
    )
    
    embed.add_field(
        name=convert_to_font("💰 заработано"),
        value=convert_to_font(f"{total_reward} монет"),
        inline=True
    )
    
    if rank_bonus > 0:
        embed.add_field(
            name=convert_to_font("🎁 бонус за ранг"),
            value=convert_to_font(f"+{rank_bonus} монет"),
            inline=True
        )
    
    embed.add_field(
        name=convert_to_font("баланс"),
        value=convert_to_font(f"{user['coins']} монет"),
        inline=False
    )
    
    embed.set_footer(text=convert_to_font("следующая работа через 1 час"))
    
    await ctx.send(embed=embed)
```

---

## 📝 Как добавить эти функции:

1. **Slots** - скопировать код в `bot.py` после других игровых команд
2. **RPS** - скопировать код в `bot.py` после slots
3. **Shop** - создать файл `py/shop_system.py` и добавить команды в `bot.py`
4. **Achievements** - создать файл `py/achievements_system.py` и добавить команды в `bot.py`
5. **Work** - скопировать код в `bot.py` после других команд

Не забудь:
- Добавить новые команды в `commands_manager.py`
- Обновить список команд через `!updatecommands`
- Протестировать каждую функцию
- Добавить поле `inventory` в структуру пользователя в `database.py`

---

**Дата:** 02.02.2026  
**Автор:** Kiro AI Assistant
