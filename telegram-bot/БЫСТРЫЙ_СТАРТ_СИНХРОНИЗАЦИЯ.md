# ⚡ Быстрый старт: Двусторонняя синхронизация

**Время:** 40 минут  
**Цель:** Запустить базовую синхронизацию Telegram → Discord

---

## 🎯 Что получишь

После выполнения этих шагов:
- ✅ События создаются при играх в Telegram
- ✅ SyncWorker обрабатывает очередь каждые 5 секунд
- ✅ ReconcileWorker проверяет расхождения каждые 15 минут
- ✅ Все транзакции логируются в БД

**Что НЕ будет работать:**
- ❌ Discord Bot (нужно создать отдельно)
- ❌ Синхронизация Discord → Telegram
- ❌ Автоматическая смена ролей

---

## 📋 Шаги

### 1. Применить миграцию (5 минут)

```bash
# Подключиться к PostgreSQL
psql $DATABASE_URL

# Применить миграцию
\i infrastructure/database/migrations/006_bidirectional_sync.sql

# Проверить таблицы
\dt sync_*
\dt transactions
\dt rank_role_mappings

# Проверить данные
SELECT COUNT(*) FROM rank_role_mappings;  # Должно быть 20
```

---

### 2. Интегрировать в main_v3.py (15 минут)

#### Добавить импорты (в начало файла):
```python
from infrastructure.database.repositories.sync_repository import SyncRepository
from domain.services.sync_service import SyncService
from application.jobs.sync_worker import SyncWorker, ReconcileWorker, CleanupWorker
```

#### Создать репозиторий (после других репозиториев):
```python
# Создаём repositories
print("\n🔧 Инициализация repositories...")
user_repo = UserRepository(db_connection.get_pool())
game_repo = GameRepository(db_connection.get_pool())
ticket_repo = TicketRepository(db_connection.get_pool())
season_repo = SeasonRepository(db_connection.get_pool())
achievement_repo = AchievementRepository(db_connection.get_pool())
discord_repo = DiscordRepository(db_connection.get_pool())
sync_repo = SyncRepository(db_connection.get_pool())  # NEW
```

#### Создать сервис (после других сервисов):
```python
# Создаём services (с кэшем)
print("🔧 Инициализация services...")
user_service = UserService(user_repo, cache)
discord_service = DiscordService(discord_repo, discord_client)
sync_service = SyncService(sync_repo, user_repo, discord_repo)  # NEW
achievement_service = AchievementService(achievement_repo, user_service, discord_service)
season_service = SeasonService(season_repo, user_service, achievement_service, discord_service)
game_service = GameService(game_repo, user_service, season_service, achievement_service)  # Обновим позже
ticket_service = TicketService(ticket_repo, user_service)
```

#### Обновить GameService (передать sync_service):
```python
game_service = GameService(
    game_repo, user_service,
    season_service, achievement_service,
    sync_service  # NEW
)
```

#### Запустить воркеры (после других фоновых задач):
```python
# Запускаем фоновую задачу очистки состояний
print("🧹 Запуск фоновых задач...")

async def cleanup_states():
    """Периодическая очистка истекших состояний"""
    while True:
        await asyncio.sleep(300)  # Каждые 5 минут
        state_manager.cleanup_expired()

async def check_season():
    """Проверка окончания сезона и обновление рангов"""
    while True:
        await asyncio.sleep(3600)  # Каждый час
        try:
            await season_service.check_season_end()
            await season_service.update_all_ranks()
        except Exception as e:
            logger.error(f"Ошибка в check_season: {e}")

async def process_discord_roles():
    """Обработка невыданных Discord ролей"""
    while True:
        await asyncio.sleep(300)  # Каждые 5 минут
        try:
            await discord_service.process_pending_role_grants()
            await discord_service.expire_old_codes()
        except Exception as e:
            logger.error(f"Ошибка в process_discord_roles: {e}")

asyncio.create_task(cleanup_states())
asyncio.create_task(check_season())
asyncio.create_task(process_discord_roles())

# NEW: Запускаем воркеры синхронизации
sync_worker = SyncWorker(sync_service, interval_seconds=5)
reconcile_worker = ReconcileWorker(sync_service, interval_minutes=15)
cleanup_worker = CleanupWorker(sync_service, interval_hours=24)

asyncio.create_task(sync_worker.start())
asyncio.create_task(reconcile_worker.start())
asyncio.create_task(cleanup_worker.start())
```

#### Обновить вывод при запуске:
```python
print("\n" + "=" * 60)
print("✅ TTFD Bot v3.0 запущен и готов к работе!")
print("   • Clean Architecture")
print("   • PostgreSQL")
print("   • Redis Cache" if cache.enabled else "   • Memory Cache (fallback)")
print("   • Domain-Driven Design")
print("   • Role-Based Access Control (RBAC)")
print("   • Centralized Callback Router")
print("   • State Manager with TTL")
print("   • Season System (30 days)")
print("   • Achievement System")
print("   • Discord Integration" if discord_client else "   • Discord Integration (disabled)")
print("   • Bidirectional Sync (Telegram ↔ Discord)")  # NEW
print("=" * 60)
```

---

### 3. Обновить GameService (10 минут)

Открыть `domain/services/game_service.py`:

#### Обновить __init__:
```python
def __init__(
    self,
    game_repo: GameRepository,
    user_repo: UserRepository,
    season_service=None,
    achievement_service=None,
    sync_service=None  # NEW
):
    self.game_repo = game_repo
    self.user_repo = user_repo
    self.season_service = season_service
    self.achievement_service = achievement_service
    self.sync_service = sync_service  # NEW
```

#### Добавить импорт (в начало файла):
```python
from datetime import datetime  # Если ещё нет
```

#### В check_guess() после update_xp:
```python
# Выдаём награды
user = await self.user_repo.get_by_id(session.user_id)
if reward_coins > 0:
    await self.user_repo.update_coins(user.id, reward_coins)
await self.user_repo.update_xp(user.id, reward_xp)

# NEW: Создаём событие синхронизации
if self.sync_service:
    await self.sync_service.create_xp_change_event(
        user_id=user.id,
        delta_xp=reward_xp,
        source="telegram",
        reason="game_guess",
        entity_id=str(session.id)
    )
    
    if reward_coins > 0:
        await self.sync_service.create_balance_change_event(
            user_id=user.id,
            delta_balance=reward_coins,
            source="telegram",
            reason="game_guess",
            entity_id=str(session.id)
        )

# Добавляем в сезонный прогресс
if self.season_service:
    # ... существующий код ...
```

#### В check_quiz_answer() после update_xp:
```python
# Выдаём награды
user = await self.user_repo.get_by_id(session.user_id)
if reward_coins > 0:
    await self.user_repo.update_coins(user.id, reward_coins)
await self.user_repo.update_xp(user.id, reward_xp)

# NEW: Создаём событие синхронизации
if self.sync_service:
    await self.sync_service.create_xp_change_event(
        user_id=user.id,
        delta_xp=reward_xp,
        source="telegram",
        reason="game_quiz",
        entity_id=str(session.id)
    )
    
    if reward_coins > 0:
        await self.sync_service.create_balance_change_event(
            user_id=user.id,
            delta_balance=reward_coins,
            source="telegram",
            reason="game_quiz",
            entity_id=str(session.id)
        )

# Добавляем в сезонный прогресс
if self.season_service:
    # ... существующий код ...
```

#### В spin_wheel() после update_xp:
```python
# Выдаём награду
reward_coins = selected_reward['coins']
reward_xp = selected_reward['xp']

if reward_coins > 0:
    await self.user_repo.update_coins(user.id, reward_coins)

if reward_xp > 0:
    await self.user_repo.update_xp(user.id, reward_xp)

# NEW: Создаём событие синхронизации
if self.sync_service:
    if reward_xp > 0:
        await self.sync_service.create_xp_change_event(
            user_id=user.id,
            delta_xp=reward_xp,
            source="telegram",
            reason="daily_spin",
            entity_id=f"spin_{int(datetime.now().timestamp())}"
        )
    
    if reward_coins > 0:
        await self.sync_service.create_balance_change_event(
            user_id=user.id,
            delta_balance=reward_coins,
            source="telegram",
            reason="daily_spin",
            entity_id=f"spin_{int(datetime.now().timestamp())}"
        )

# Добавляем в сезонный прогресс
if self.season_service:
    # ... существующий код ...
```

---

### 4. Тестировать (10 минут)

#### Запустить бота:
```bash
python main_v3.py
```

#### Проверить логи:
Должны быть сообщения:
```
🚀 SyncWorker запущен: interval=5s, batch_size=100
🚀 ReconcileWorker запущен: interval=15m, batch_size=100
🚀 CleanupWorker запущен: interval=24h, retention=30d
✅ TTFD Bot v3.0 запущен и готов к работе!
   • Bidirectional Sync (Telegram ↔ Discord)
```

#### Сыграть в игру:
```
/games → Угадай число → Сделать ставку → Угадать
```

#### Проверить события в БД:
```bash
psql $DATABASE_URL

# Последние события
SELECT 
    id, event_type, source, status, 
    payload->>'delta_xp' as delta_xp,
    created_at
FROM sync_events 
ORDER BY created_at DESC 
LIMIT 5;

# Должно быть событие с:
# - event_type: xp_change
# - source: telegram
# - status: pending (сначала) → completed (через 5 секунд)

# Проверить транзакции
SELECT * FROM transactions ORDER BY created_at DESC LIMIT 5;

# Проверить статистику
SELECT status, COUNT(*) FROM sync_events GROUP BY status;
```

---

## ✅ Готово!

Теперь у тебя работает:
- ✅ Создание событий при играх
- ✅ Автоматическая обработка очереди
- ✅ Логирование транзакций
- ✅ Reconcile для проверки расхождений

---

## 🔍 Отладка

### Если события не создаются:
```python
# Проверить что sync_service передан в GameService
print(f"GameService.sync_service: {game_service.sync_service}")

# Добавить логирование в check_guess()
if self.sync_service:
    print("Creating sync event...")
    event = await self.sync_service.create_xp_change_event(...)
    print(f"Event created: {event.id}")
```

### Если события не обрабатываются:
```bash
# Проверить логи SyncWorker
# Должны быть сообщения каждые 5 секунд:
# "📋 SyncWorker: обработка N событий"

# Проверить статус событий
psql $DATABASE_URL
SELECT status, COUNT(*) FROM sync_events GROUP BY status;

# Если все pending - проверить ошибки
SELECT id, error_message FROM sync_events WHERE status = 'failed';
```

### Если нужна принудительная обработка:
```python
# В main_v3.py добавить команду для отладки
@app.add_handler(CommandHandler("sync_now", sync_now_command))

async def sync_now_command(update, context):
    await sync_worker.process_now()
    await update.message.reply_text("✅ Синхронизация запущена")
```

---

## 📚 Дополнительно

### Следующие шаги:
1. Создать Discord Bot (3-4 часа)
2. Реализовать команды /link, /unlink, /profile
3. Реализовать обработчики on_voice_state_update, on_message
4. Интегрировать с SyncService

### Документация:
- `ГОТОВО_ДВУСТОРОННЯЯ_СИНХРОНИЗАЦИЯ_ЭТАП_1.md` - полная сводка
- `СЛЕДУЮЩИЕ_ШАГИ_СИНХРОНИЗАЦИЯ.md` - детальные инструкции
- `АРХИТЕКТУРА_ДВУСТОРОННЕЙ_СИНХРОНИЗАЦИИ.md` - архитектура

---

**Время:** 40 минут  
**Результат:** Базовая синхронизация работает! 🎉
