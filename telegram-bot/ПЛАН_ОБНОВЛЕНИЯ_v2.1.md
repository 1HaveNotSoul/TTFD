# План обновления Telegram бота v2.1

## Задача №1: Полноценная тикет-система с FSM

### Изменения в файлах:

1. **utils/tickets.py** - расширение функционала
   - Добавить поля: `priority` (low/medium/high), `assigned_to` (admin_id или null)
   - Добавить file locking для атомарной записи
   - Функции фильтрации тикетов по статусу

2. **handlers/tickets.py** (НОВЫЙ)
   - FSM для создания тикета: выбор категории → ввод текста → подтверждение
   - Обработчики для пользователей: создание, просмотр, ответ, закрытие
   - Обработчики для админов: список, фильтры, взять в работу, ответить, закрыть
   - Уведомления пользователям при ответе админа

3. **main.py**
   - Добавить ConversationHandler для FSM тикетов
   - Зарегистрировать новые обработчики

4. **handlers/buttons.py**
   - Обновить callback_data для тикетов (стандартизация)
   - Добавить кнопки для админ-панели тикетов

## Задача №2: Раздел "🎮 Игры"

### Новые файлы:

1. **handlers/games.py** (НОВЫЙ)
   - Обработчики игр: меню, выбор игры, игровой процесс
   - FSM для игр с состояниями

2. **utils/games.py** (НОВЫЙ)
   - Логика игр: "Угадай число", "Квиз", "Ежедневный спин"
   - Проверка кулдаунов, ставок, наград
   - Обновление XP/coins через database.py

3. **database.py**
   - Добавить поля для игр: `last_spin`, `games_stats`
   - Функции для работы с игровой статистикой

### Изменения в существующих файлах:

1. **main.py**
   - Добавить ConversationHandler для игр
   - Зарегистрировать обработчики игр

2. **handlers/buttons.py**
   - Добавить кнопку "🎮 Игры" в главное меню
   - Callback handlers для игр

## Стандартизация callback_data:

```
Формат: <модуль>_<действие>_<параметры>

Примеры:
- ticket_create_start
- ticket_view_123
- ticket_close_123
- ticket_admin_list_open
- game_menu
- game_guess_start
- game_quiz_start
- game_spin_start
- shop_buy_1
```

## Edge-cases:

1. Отмена действия - кнопка "❌ Отмена" на каждом шаге
2. Таймаут - ConversationHandler с timeout
3. Неверный ввод - валидация + повторный запрос
4. Повторные нажатия - проверка состояния перед действием
5. Недостаточно монет - проверка баланса перед ставкой
6. Конфликты записи JSON - file locking (fcntl на Linux, msvcrt на Windows)

## Миграция на SQLite (опционально):

Если JSON станет проблемой - предложу миграцию на SQLite с:
- Транзакциями для атомарности
- Индексами для быстрого поиска
- Скриптом миграции данных
