# ⚡ БЫСТРЫЙ СТАРТ: ПРИВЯЗКА DISCORD

**Для пользователей:** Как привязать Discord к Telegram боту

---

## 🎯 ДЛЯ ПОЛЬЗОВАТЕЛЕЙ

### 1. Узнай свой Discord ID

1. Открой Discord
2. Настройки → Расширенные
3. Включи "Режим разработчика"
4. Нажми ПКМ на свой профиль
5. Выбери "Копировать ID"

### 2. Отправь команду в Telegram боте

```
/link 123456789012345678
```
(замени на свой Discord ID)

### 3. Готово! ✅

Баланс теперь синхронизирован между Telegram и Discord!

---

## 🔧 ДЛЯ РАЗРАБОТЧИКА

### Интеграция улучшенной команды

**1. Откройте `main.py`:**

Добавьте импорт:
```python
from handlers.link_discord import (
    link_discord_command, 
    check_link_command, 
    unlink_discord_command
)
```

**2. Замените регистрацию:**

Было:
```python
app.add_handler(CommandHandler("link", link_command))
```

Стало:
```python
app.add_handler(CommandHandler("link", link_discord_command))
app.add_handler(CommandHandler("checklink", check_link_command))
app.add_handler(CommandHandler("unlink", unlink_discord_command))
```

**3. Деплой:**

```bash
git add .
git commit -m "Интеграция улучшенной команды /link"
git push
```

---

## 📊 НОВЫЕ КОМАНДЫ

- `/link <discord_id>` - привязать Discord
- `/checklink` - проверить статус привязки
- `/unlink` - отвязать Discord (только админы)

---

**Готово!** 🎉
