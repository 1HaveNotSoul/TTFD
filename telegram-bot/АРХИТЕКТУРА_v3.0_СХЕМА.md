# 🏗️ Архитектура TTFD Platform v3.0

## Визуальная схема слоёв

```
┌─────────────────────────────────────────────────────────────────┐
│                      PRESENTATION LAYER                          │
│                                                                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │   Telegram   │  │   Web API    │  │   Discord    │          │
│  │   Bot UI     │  │   (FastAPI)  │  │   Webhook    │          │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘          │
│         │                  │                  │                   │
└─────────┼──────────────────┼──────────────────┼──────────────────┘
          │                  │                  │
          ▼                  ▼                  ▼
┌─────────────────────────────────────────────────────────────────┐
│                     APPLICATION LAYER                            │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                      HANDLERS                             │  │
│  │  • UserHandler    • GameHandler    • TicketHandler       │  │
│  │  • ShopHandler    • AdminHandler   • EventHandler        │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │ Middlewares  │  │     Jobs     │  │   Events     │          │
│  │ • Auth       │  │ • Scheduler  │  │ • EventBus   │          │
│  │ • RateLimit  │  │ • Cron       │  │ • Listeners  │          │
│  │ • Logging    │  │ • Tasks      │  │ • Handlers   │          │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘          │
│         │                  │                  │                   │
└─────────┼──────────────────┼──────────────────┼──────────────────┘
          │                  │                  │
          ▼                  ▼                  ▼
┌─────────────────────────────────────────────────────────────────┐
│                       DOMAIN LAYER                               │
│                    (Бизнес-логика)                               │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                      SERVICES                             │  │
│  │  • UserService         • EconomyService                   │  │
│  │  • GameService         • TicketService                    │  │
│  │  • PermissionService   • AchievementService               │  │
│  │  • AntiAbuseService    • NotificationService              │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                       MODELS                              │  │
│  │  • User      • Wallet      • Game      • Ticket           │  │
│  │  • Item      • Achievement • Tournament • Season          │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                   DOMAIN EVENTS                           │  │
│  │  • UserCreated    • GameFinished    • TicketClosed        │  │
│  │  • LevelUp        • AchievementUnlocked                   │  │
│  └──────┬───────────────────────────────────────────────────┘  │
│         │                                                         │
└─────────┼─────────────────────────────────────────────────────────┘
          │
          ▼
┌─────────────────────────────────────────────────────────────────┐
│                   INFRASTRUCTURE LAYER                           │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                   REPOSITORIES                            │  │
│  │  • UserRepository      • WalletRepository                 │  │
│  │  • GameRepository      • TicketRepository                 │  │
│  │  • InventoryRepository • AchievementRepository            │  │
│  └──────┬───────────────────────────────────────────────────┘  │
│         │                                                         │
│  ┌──────▼───────┐  ┌──────────────┐  ┌──────────────┐          │
│  │  PostgreSQL  │  │     Redis    │  │   External   │          │
│  │              │  │              │  │              │          │
│  │ • Users      │  │ • Cache      │  │ • OpenAI     │          │
│  │ • Wallets    │  │ • Sessions   │  │ • Discord    │          │
│  │ • Games      │  │ • RateLimit  │  │ • Analytics  │          │
│  │ • Tickets    │  │ • Queues     │  │              │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```

---

## Поток данных: Пример "Профиль пользователя"

```
1. Пользователь → /profile
   │
   ▼
2. Telegram Bot API
   │
   ▼
3. ProfileHandler (Application Layer)
   │  - Извлекает telegram_id
   │  - Проверяет права (Middleware)
   │
   ▼
4. UserService (Domain Layer)
   │  - Бизнес-логика: получить профиль
   │  - Проверить кэш
   │
   ▼
5. UserRepository (Infrastructure Layer)
   │  - Запрос к PostgreSQL
   │  - SELECT * FROM users WHERE telegram_id = ?
   │
   ▼
6. PostgreSQL
   │  - Возвращает данные
   │
   ▼
7. UserService
   │  - Обогащает данные (ранг, прогресс)
   │  - Сохраняет в кэш (Redis)
   │
   ▼
8. ProfileHandler
   │  - Форматирует сообщение
   │  - Создаёт клавиатуру
   │
   ▼
9. Telegram Bot API
   │  - Отправляет сообщение пользователю
   │
   ▼
10. Пользователь видит профиль
```

---

## Зависимости между слоями

```
Presentation → Application → Domain → Infrastructure
     ↓              ↓           ↓            ↓
  Telegram      Handlers    Services    Repositories
   FastAPI      Middleware   Models      PostgreSQL
   Discord      Jobs         Events      Redis
                                         External APIs
```

**Правила:**
- ✅ Presentation может вызывать Application
- ✅ Application может вызывать Domain
- ✅ Domain может вызывать Infrastructure
- ❌ Infrastructure НЕ может вызывать Domain
- ❌ Domain НЕ может вызывать Application
- ❌ Application НЕ может напрямую вызывать Infrastructure (только через Domain)

---

## Dependency Injection

```python
# main.py
async def main():
    # 1. Создаём инфраструктуру
    db_pool = await asyncpg.create_pool(DATABASE_URL)
    redis_client = await redis.from_url(REDIS_URL)
    
    # 2. Создаём repositories
    user_repo = UserRepository(db_pool)
    wallet_repo = WalletRepository(db_pool)
    game_repo = GameRepository(db_pool)
    
    # 3. Создаём cache
    cache = RedisCache(redis_client)
    
    # 4. Создаём services (внедряем зависимости)
    user_service = UserService(user_repo, cache)
    economy_service = EconomyService(wallet_repo, cache)
    game_service = GameService(game_repo, user_service, economy_service)
    
    # 5. Создаём handlers (внедряем services)
    profile_handler = ProfileHandler(user_service)
    game_handler = GameHandler(game_service)
    
    # 6. Регистрируем handlers в боте
    app = Application.builder().token(BOT_TOKEN).build()
    app.add_handler(CommandHandler("profile", profile_handler.handle))
    app.add_handler(CommandHandler("game", game_handler.handle))
    
    # 7. Запускаем
    await app.run_polling()
```

---

## Преимущества новой архитектуры

### 1. Тестируемость
```python
# Легко мокать зависимости
@pytest.mark.asyncio
async def test_user_service():
    mock_repo = AsyncMock()
    mock_cache = AsyncMock()
    
    service = UserService(mock_repo, mock_cache)
    user = await service.get_user("123")
    
    mock_repo.get_by_telegram_id.assert_called_once_with("123")
```

### 2. Переиспользование
```python
# Один service для разных интерфейсов
user_service = UserService(user_repo, cache)

# Telegram handler
telegram_handler = TelegramProfileHandler(user_service)

# Web API endpoint
@app.get("/api/users/{telegram_id}")
async def get_user(telegram_id: str):
    return await user_service.get_user(telegram_id)

# Discord webhook
discord_handler = DiscordProfileHandler(user_service)
```

### 3. Масштабируемость
```python
# Легко добавить кэширование
class UserService:
    async def get_user(self, telegram_id: str):
        # Проверяем кэш
        cached = await self.cache.get(f"user:{telegram_id}")
        if cached:
            return cached
        
        # Загружаем из БД
        user = await self.user_repo.get_by_telegram_id(telegram_id)
        
        # Сохраняем в кэш
        await self.cache.set(f"user:{telegram_id}", user, ttl=300)
        
        return user
```

### 4. Изоляция изменений
```python
# Меняем БД с JSON на PostgreSQL
# Нужно изменить только Repository
# Services и Handlers остаются без изменений

class UserRepository:
    # Было: JSON
    def get_user(self, telegram_id):
        with open('users.json') as f:
            data = json.load(f)
        return data.get(telegram_id)
    
    # Стало: PostgreSQL
    async def get_user(self, telegram_id):
        async with self.pool.acquire() as conn:
            row = await conn.fetchrow(
                "SELECT * FROM users WHERE telegram_id = $1",
                telegram_id
            )
        return User.from_db_row(row)
```

---

## Следующие шаги

1. Изучить `PLATFORM_EVOLUTION_ROADMAP.md` - полный план
2. Изучить `БЫСТРЫЙ_СТАРТ_v3.0.md` - пошаговая инструкция
3. Начать с Этапа 1: Архитектурный рефакторинг
4. Двигаться итеративно: v3.0 → v3.5 → v4.0 → v5.0

Готов к старту? 🚀
