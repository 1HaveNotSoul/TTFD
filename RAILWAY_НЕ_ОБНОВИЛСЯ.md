# ⚠️ ПРОБЛЕМА: Railway не подхватил изменения

## 🔍 Что произошло

Railway всё ещё использует **старую версию кода** где импортируется `asyncpg`.

**Доказательство из логов:**
```
❌ Ошибка регистрации команд привязки: No module named 'asyncpg'
File "/app/py/discord_link_code.py", line 9, in <module>
import asyncpg
```

**Но в GitHub уже исправлено:**
- Коммит d9cc663: убран импорт asyncpg
- Коммит 604cf07: исправлена ошибка автообновлений
- Коммит dd7cff1: добавлена документация

## 🚀 Решение

### Вариант 1: Вручную задеплоить на Railway

1. **Открой Railway Dashboard**
   - Зайди на railway.app
   - Выбери проект Discord Bot

2. **Проверь последний деплой**
   - Deployments → Latest
   - Проверь какой коммит задеплоен
   - Должен быть: `dd7cff1` или новее

3. **Если старый коммит - задеплой вручную**
   - Settings → Service
   - Нажми "Redeploy" или "Deploy"
   - Выбери ветку `main`
   - Подожди 1-2 минуты

### Вариант 2: Проверь подключение к GitHub

1. **Открой Settings → Service**
   - Проверь что подключен правильный репозиторий
   - Должно быть: `1HaveNotSoul/TTFD`
   - Ветка: `main`

2. **Проверь Auto Deploy**
   - Settings → Service → Auto Deploy
   - Должно быть включено (ON)

3. **Если выключено - включи**
   - Переключи на ON
   - Сохрани изменения

### Вариант 3: Очисти кеш Railway

1. **Открой Settings → Service**
2. **Найди "Clear Cache"**
3. **Нажми "Clear Cache"**
4. **Задеплой заново**

## 📋 Как проверить что обновилось

### 1. Проверь коммит в Railway

**Railway → Deployments → Latest**

Должен быть один из:
- `dd7cff1` - docs: добавлена финальная сводка
- `604cf07` - fix: исправлена ошибка длинных сообщений
- `d9cc663` - fix: исправлена регистрация /getcode

### 2. Проверь логи Railway

**Должно быть:**
```
✅ Команды привязки через код зарегистрированы (/getcode, /checklink, /unlink)
✅ Синхронизировано X slash команд с Discord (global sync)
⏰ Команды появятся в течение 1 часа
```

**НЕ должно быть:**
```
❌ No module named 'asyncpg'
❌ 403 Forbidden (error code: 50001): Missing Access
```

### 3. Проверь файл на Railway

Если есть доступ к файлам Railway:
```bash
cat /app/py/discord_link_code.py | head -15
```

Должно быть:
```python
import discord
from discord import app_commands
from datetime import datetime, timedelta
import secrets
import logging
```

НЕ должно быть:
```python
import asyncpg  # ❌ Этого не должно быть!
```

## ⚠️ Важно про команду /getcode

### Это SLASH команда!

**Неправильно:**
```
/getcode  (как обычная команда в чате)
```

**Правильно:**
1. Начни вводить `/` в Discord
2. Появится меню slash команд
3. Найди `/getcode` в списке
4. Нажми на неё или нажми Tab
5. Нажми Enter

### Почему не работает как обычная команда?

Из логов:
```
discord.ext.commands.errors.CommandNotFound: Command "getcode" is not found
```

Это потому что `/getcode` - это **slash команда** (app_commands), а не обычная команда (commands).

**Slash команды:**
- Регистрируются через `@bot.tree.command`
- Появляются в меню команд Discord
- Используются через меню `/`

**Обычные команды:**
- Регистрируются через `@bot.command`
- Используются как обычный текст
- Пример: `!help`, `!profile`

## 🔧 Если всё равно не работает

### Проблема 1: Railway не деплоит автоматически

**Решение:**
1. Settings → Service → Auto Deploy → ON
2. Или деплой вручную каждый раз

### Проблема 2: GitHub не подключен

**Решение:**
1. Settings → Service → Source
2. Connect to GitHub
3. Выбери репозиторий `1HaveNotSoul/TTFD`
4. Выбери ветку `main`

### Проблема 3: Кеш Railway

**Решение:**
1. Settings → Clear Cache
2. Redeploy

### Проблема 4: Неправильная ветка

**Решение:**
1. Settings → Service → Branch
2. Убедись что выбрана `main`
3. Redeploy

## 📊 Следующие шаги

1. ✅ Проверь коммит в Railway Deployments
2. ✅ Если старый - задеплой вручную
3. ✅ Проверь логи - не должно быть ошибки asyncpg
4. ⏰ Подожди до 1 часа (global sync)
5. 🎮 Используй `/getcode` через меню slash команд (не как обычную команду!)

---

**Дата:** 09.02.2026  
**Проблема:** Railway не подхватил изменения  
**Решение:** Вручную задеплоить или проверить Auto Deploy
