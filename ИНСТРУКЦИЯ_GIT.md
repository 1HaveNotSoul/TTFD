# 📚 Git Инструкция - TTFD Project

Полное руководство по работе с Git для проекта TTFD.

## 🚀 Быстрый старт

### 1. Организация проектов

```cmd
cd C:\Users\brawl\OneDrive\Desktop\папки\TTFD
.\ОРГАНИЗОВАТЬ_ПРОЕКТЫ.bat
```

Это скопирует все проекты в единую структуру:
```
TTFD/
├── website/          (из TTFD-Website)
├── discord-bot/      (из TTFD-Discord)
└── cleaner/          (из TTFD-Cleaner)
```

### 2. Инициализация Git

```bash
cd TTFD
git init
git branch -M main
```

### 3. Первый коммит

```bash
git add .
git commit -m "Initial commit: TTFD unified project"
```

### 4. Создание GitHub репозитория

1. Перейдите на https://github.com/new
2. Название: `TTFD`
3. Description: `The First Descendants - Unified Project (Website + Discord Bot + Cleaner)`
4. Public или Private (на ваш выбор)
5. **НЕ** добавляйте README, .gitignore, license (уже есть)
6. Create repository

### 5. Подключение к GitHub

```bash
git remote add origin https://github.com/yourusername/TTFD.git
git push -u origin main
```

## 📝 Основные команды Git

### Проверка статуса

```bash
git status
```

Показывает:
- Изменённые файлы
- Неотслеживаемые файлы
- Текущую ветку

### Добавление файлов

```bash
# Добавить все файлы
git add .

# Добавить конкретный файл
git add website/app.py

# Добавить папку
git add discord-bot/
```

### Коммит изменений

```bash
# С сообщением
git commit -m "feat: добавлена новая команда !dice"

# С подробным описанием
git commit -m "feat: добавлена новая команда !dice" -m "Команда позволяет бросить кубик от 1 до 6"
```

### Отправка на GitHub

```bash
# Первый раз
git push -u origin main

# Последующие разы
git push
```

### Получение изменений

```bash
# Скачать изменения
git pull
```

## 🌿 Работа с ветками

### Создание ветки

```bash
# Создать и переключиться
git checkout -b feature/new-command

# Или отдельно
git branch feature/new-command
git checkout feature/new-command
```

### Переключение веток

```bash
git checkout main
git checkout feature/new-command
```

### Слияние веток

```bash
# Переключиться на main
git checkout main

# Слить feature ветку
git merge feature/new-command
```

### Удаление ветки

```bash
# Локально
git branch -d feature/new-command

# На GitHub
git push origin --delete feature/new-command
```

## 📋 Conventional Commits

Используйте стандартизированные сообщения коммитов:

### Типы коммитов

```bash
feat:     Новая функция
fix:      Исправление бага
docs:     Документация
style:    Форматирование кода
refactor: Рефакторинг
test:     Тесты
chore:    Обновление зависимостей, конфигурации
```

### Примеры

```bash
# Новая функция
git commit -m "feat: добавлена команда !dice в Discord бота"

# Исправление
git commit -m "fix: исправлена ошибка в системе рангов"

# Документация
git commit -m "docs: обновлена инструкция по деплою"

# Стиль
git commit -m "style: форматирование кода в app.py"

# Рефакторинг
git commit -m "refactor: переработана система тикетов"

# Тесты
git commit -m "test: добавлены тесты для OAuth"

# Обслуживание
git commit -m "chore: обновлены зависимости Flask"
```

### С областью (scope)

```bash
git commit -m "feat(discord-bot): добавлена команда !dice"
git commit -m "fix(website): исправлен OAuth redirect"
git commit -m "docs(cleaner): обновлён README"
```

## 🔄 Workflow

### Feature Branch Workflow

```bash
# 1. Создать ветку для новой функции
git checkout -b feature/shop-system

# 2. Внести изменения
# ... редактирование файлов ...

# 3. Коммит изменений
git add .
git commit -m "feat: добавлена система магазина"

# 4. Отправить на GitHub
git push -u origin feature/shop-system

# 5. Создать Pull Request на GitHub
# GitHub → Pull Requests → New Pull Request

# 6. После одобрения - слить в main
git checkout main
git merge feature/shop-system
git push

# 7. Удалить ветку
git branch -d feature/shop-system
git push origin --delete feature/shop-system
```

## 🚫 .gitignore

Файлы, которые НЕ нужно коммитить:

```gitignore
# Секреты
.env
*.env
!.env.example

# Python
__pycache__/
*.pyc
venv/

# Database
*.db
*.json
!.env.example

# Build
build/
dist/
*.exe
!cleaner/TTFD.Cleaner.Cli.exe

# IDE
.vscode/
.idea/
```

### Проверка .gitignore

```bash
# Проверить что будет закоммичено
git status

# Проверить конкретный файл
git check-ignore -v .env
```

## 🔐 Безопасность

### ⚠️ НИКОГДА не коммитьте:

- `.env` файлы с токенами
- `DISCORD_TOKEN`
- `DISCORD_CLIENT_SECRET`
- `SECRET_KEY`
- `DATABASE_URL` с паролями
- Приватные ключи

### ✅ Что коммитить:

- `.env.example` (шаблон без секретов)
- Исходный код
- Документацию
- Конфигурационные файлы (без секретов)

### Если случайно закоммитили секрет:

```bash
# 1. Удалить файл из Git (но оставить локально)
git rm --cached .env

# 2. Добавить в .gitignore
echo ".env" >> .gitignore

# 3. Коммит
git commit -m "chore: remove .env from git"

# 4. ВАЖНО: Смените все секреты!
# - Сгенерируйте новый DISCORD_TOKEN
# - Сгенерируйте новый SECRET_KEY
# - Обновите на Render
```

## 📦 Releases

### Создание релиза

```bash
# 1. Создать тег
git tag -a v2.0.0 -m "Release v2.0.0: Unified TTFD Project"

# 2. Отправить тег
git push origin v2.0.0

# 3. Создать Release на GitHub
# GitHub → Releases → Create a new release
# - Tag: v2.0.0
# - Title: TTFD v2.0.0
# - Description: список изменений
# - Attach: cleaner/dist/TTFD-Cleaner-Menu.exe
```

### Semantic Versioning

```
v2.0.0
│ │ │
│ │ └─ Patch (исправления)
│ └─── Minor (новые функции, обратная совместимость)
└───── Major (breaking changes)
```

Примеры:
- `v2.0.0` → `v2.0.1` - исправление бага
- `v2.0.1` → `v2.1.0` - новая функция
- `v2.1.0` → `v3.0.0` - breaking change

## 🔍 Полезные команды

### История коммитов

```bash
# Последние коммиты
git log

# Компактный вид
git log --oneline

# С графом веток
git log --graph --oneline --all

# Последние 5 коммитов
git log -5
```

### Отмена изменений

```bash
# Отменить изменения в файле (до add)
git checkout -- website/app.py

# Отменить git add
git reset HEAD website/app.py

# Отменить последний коммит (сохранить изменения)
git reset --soft HEAD~1

# Отменить последний коммит (удалить изменения)
git reset --hard HEAD~1
```

### Просмотр изменений

```bash
# Изменения в рабочей директории
git diff

# Изменения в staged файлах
git diff --staged

# Изменения в конкретном файле
git diff website/app.py
```

### Поиск

```bash
# Поиск в коммитах
git log --grep="discord"

# Поиск в коде
git grep "DISCORD_TOKEN"

# Кто изменил строку
git blame website/app.py
```

## 🤝 Collaboration

### Клонирование репозитория

```bash
git clone https://github.com/yourusername/TTFD.git
cd TTFD
```

### Обновление форка

```bash
# Добавить upstream
git remote add upstream https://github.com/original/TTFD.git

# Получить изменения
git fetch upstream

# Слить в main
git checkout main
git merge upstream/main
```

### Pull Request

1. Fork репозитория на GitHub
2. Клонировать свой fork
3. Создать ветку
4. Внести изменения
5. Push в свой fork
6. Создать Pull Request на GitHub

## 🐛 Troubleshooting

### Конфликты при merge

```bash
# 1. Git покажет конфликтующие файлы
git status

# 2. Откройте файлы и разрешите конфликты
# Найдите маркеры:
# <<<<<<< HEAD
# ваш код
# =======
# чужой код
# >>>>>>> branch

# 3. После разрешения
git add .
git commit -m "fix: resolve merge conflicts"
```

### Отменить push (ОПАСНО!)

```bash
# Только если никто не успел pull!
git push --force
```

### Забыли добавить файл в коммит

```bash
# Добавить файл
git add forgotten-file.py

# Изменить последний коммит
git commit --amend --no-edit
```

## 📊 GitHub Actions (Future)

```yaml
# .github/workflows/deploy.yml
name: Deploy
on:
  push:
    branches: [main]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Deploy to Render
        run: echo "Deploy"
```

## 📝 Чеклист перед коммитом

- [ ] Код работает локально
- [ ] Нет `.env` файлов в коммите
- [ ] Обновлён `.gitignore` если нужно
- [ ] Написано понятное сообщение коммита
- [ ] Проверен `git status`
- [ ] Проверен `git diff`

## 📝 Чеклист перед push

- [ ] Все коммиты имеют понятные сообщения
- [ ] Нет секретов в коде
- [ ] Код протестирован
- [ ] Документация обновлена
- [ ] README актуален

---

**Готово!** Теперь вы знаете как работать с Git для проекта TTFD! 🚀

## 🔗 Полезные ссылки

- [Git Documentation](https://git-scm.com/doc)
- [GitHub Guides](https://guides.github.com/)
- [Conventional Commits](https://www.conventionalcommits.org/)
- [Semantic Versioning](https://semver.org/)
