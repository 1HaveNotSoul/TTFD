# 🚀 Быстрый старт Discord бота

## Шаг 1: Создай Discord бота

1. Перейди на https://discord.com/developers/applications
2. Нажми "New Application"
3. Дай имя боту и создай
4. Перейди в раздел "Bot"
5. Нажми "Reset Token" и скопируй токен (ВАЖНО: сохрани его!)
6. Включи все "Privileged Gateway Intents":
   - ✅ Presence Intent
   - ✅ Server Members Intent
   - ✅ Message Content Intent
7. Перейди в "OAuth2" → "URL Generator"
8. Выбери:
   - Scopes: `bot`, `applications.commands`
   - Bot Permissions: `Administrator` (или выбери нужные права)
9. Скопируй ссылку внизу и открой в браузере
10. Добавь бота на свой сервер

## Шаг 2: Получи ID сервера

1. В Discord включи режим разработчика:
   - Настройки → Расширенные → Режим разработчика
2. Правый клик на свой сервер → "Копировать ID сервера"

## Шаг 3: Настрой бота

1. Создай файл `.env` (скопируй из `.env.example`):
```bash
copy .env.example .env
```

2. Открой `.env` и заполни:
```env
DISCORD_TOKEN=твой_токен_бота
GUILD_ID=id_твоего_сервера
WEB_PORT=5000
SECRET_KEY=любая_случайная_строка_123456
```

## Шаг 4: Установи зависимости

```bash
pip install -r requirements.txt
```

## Шаг 5: Запусти бота

```bash
python main.py
```

Готово! Бот запущен:
- 🤖 Discord бот работает
- 🌐 Веб-панель: http://localhost:5000

## 📋 Команды бота

- `!ping` - Проверка задержки
- `!stats` - Статистика бота
- `!info` - Информация о боте
- `!help` - Список команд

Также доступны slash команды: `/ping`, `/stats`

## 🌐 Деплой на сервер (24/7)

### Простой способ - Screen (Linux/VPS)

```bash
# Установи screen
sudo apt install screen

# Создай сессию
screen -S discord-bot

# Запусти бота
python main.py

# Отключись от сессии: Ctrl+A, затем D
# Подключись обратно: screen -r discord-bot
```

### Systemd Service (Linux)

Создай `/etc/systemd/system/discord-bot.service`:

```ini
[Unit]
Description=Discord Bot
After=network.target

[Service]
Type=simple
User=твой_пользователь
WorkingDirectory=/путь/к/discord-bot
ExecStart=/usr/bin/python3 /путь/к/discord-bot/main.py
Restart=always

[Install]
WantedBy=multi-user.target
```

Запуск:
```bash
sudo systemctl enable discord-bot
sudo systemctl start discord-bot
```

## ❓ Проблемы?

### Бот не запускается
- Проверь токен в `.env`
- Убедись что установлены все зависимости: `pip install -r requirements.txt`

### Бот оффлайн
- Проверь что токен правильный
- Убедись что бот добавлен на сервер
- Проверь что включены все Intents в настройках бота

### Веб-панель не открывается
- Проверь что порт 5000 свободен
- Попробуй другой порт в `.env`

## 📞 Поддержка

Проблемы? Пиши: @bxdsun [TG]
