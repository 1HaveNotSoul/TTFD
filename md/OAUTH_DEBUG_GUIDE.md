# 🔍 Отладка Discord OAuth

## ✅ Что я исправил:

1. ✅ Добавил импорт `datetime` в `discord_oauth.py`
2. ✅ Улучшил обработку ошибок в OAuth endpoints
3. ✅ Добавил подробное логирование
4. ✅ Добавил отображение flash сообщений на странице логина

---

## 🔧 Что нужно проверить:

### 1. Проверь переменные в Render

Должны быть все 3:
```
DISCORD_CLIENT_ID = 1441498894008582354
DISCORD_CLIENT_SECRET = <твой_секрет>
DISCORD_REDIRECT_URI = https://ttfd.onrender.com/auth/discord/callback
```

⚠️ **ВАЖНО:** Если ты публиковал Client Secret на скриншоте - СБРОСЬ ЕГО!

### 2. Проверь Discord Developer Portal

1. Открой https://discord.com/developers/applications
2. Выбери приложение (ID: 1441498894008582354)
3. OAuth2 → Redirects
4. Должен быть: `https://ttfd.onrender.com/auth/discord/callback`
5. Если нет - добавь и Save Changes

### 3. Проверь логи Render

1. Render Dashboard → твой сервис
2. Logs (справа вверху)
3. Нажми "Войти через Discord" на сайте
4. Смотри логи - должны появиться сообщения:
   ```
   ✅ Редирект на Discord OAuth: ...
   📥 Получен callback от Discord
   ✅ Создан новый аккаунт: ... (или)
   ✅ Вход выполнен: ...
   ```

---

## 🐛 Возможные ошибки и решения:

### Ошибка: "Invalid state"
**Причина:** Проблема с сессиями Flask
**Решение:** 
1. Проверь что `SECRET_KEY` установлен в Render
2. Попробуй очистить cookies браузера
3. Попробуй в режиме инкогнито

### Ошибка: "No code provided"
**Причина:** Discord не вернул код авторизации
**Решение:**
1. Проверь что Redirect URI правильный в Discord Portal
2. Проверь что `DISCORD_REDIRECT_URI` правильный в Render
3. Они должны ТОЧНО совпадать!

### Ошибка: "Failed to exchange code"
**Причина:** Неправильный Client Secret или Redirect URI
**Решение:**
1. Проверь `DISCORD_CLIENT_SECRET` в Render
2. Проверь что секрет не истёк (если публиковал - сбрось)
3. Проверь логи Render для деталей

### Ошибка: "Failed to create session"
**Причина:** Проблема с базой данных
**Решение:**
1. Проверь что `DATABASE_URL` правильный
2. Проверь что PostgreSQL база создана
3. Проверь логи для SQL ошибок

---

## 📝 Пошаговая отладка:

### Шаг 1: Проверь что кнопка работает
1. Открой https://ttfd.onrender.com/login
2. Открой DevTools (F12)
3. Вкладка Console
4. Нажми "Войти через Discord"
5. Должен быть редирект на discord.com

### Шаг 2: Проверь Discord авторизацию
1. На странице Discord нажми "Авторизовать"
2. Должен быть редирект обратно на ttfd.onrender.com
3. Если ошибка - смотри текст ошибки

### Шаг 3: Проверь логи Render
1. Render Dashboard → Logs
2. Ищи строки с ❌ (ошибки)
3. Ищи строки с ✅ (успех)
4. Скопируй ошибку если есть

### Шаг 4: Проверь базу данных
1. Render Dashboard → PostgreSQL
2. Connect → External Connection
3. Используй psql или pgAdmin
4. Проверь таблицы: `accounts`, `sessions`

---

## 🔄 После исправлений:

1. Загрузи код на GitHub:
```bash
cd Desktop/папки/TTFD-Website
git add .
git commit -m "Fix Discord OAuth with better error handling"
git push
```

2. Render автоматически обновит сайт (2-3 минуты)

3. Попробуй снова войти через Discord

4. Если не работает - скинь логи из Render

---

## 📊 Чеклист:

- [ ] `DISCORD_CLIENT_ID` добавлен в Render
- [ ] `DISCORD_CLIENT_SECRET` добавлен в Render (и не истёк!)
- [ ] `DISCORD_REDIRECT_URI` добавлен в Render
- [ ] Redirect URI добавлен в Discord Developer Portal
- [ ] Redirect URI в Render и Discord ТОЧНО совпадают
- [ ] Код загружен на GitHub
- [ ] Render завершил деплой
- [ ] Логи Render не показывают ошибок
- [ ] База данных работает

---

## 🆘 Если всё ещё не работает:

Скинь мне:
1. Скриншот логов Render (когда нажимаешь кнопку)
2. Скриншот ошибки на сайте (если есть)
3. Скриншот Discord Developer Portal → OAuth2 → Redirects

И я помогу найти проблему!
