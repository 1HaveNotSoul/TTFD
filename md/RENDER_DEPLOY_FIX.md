# 🔧 Исправление деплоя на Render

## ❌ Проблема
Изменения на GitHub не подтягиваются автоматически на Render.

## ✅ Решение

### Вариант 1: Ручной деплой (быстро)

1. **Открой Render Dashboard:**
   ```
   https://dashboard.render.com
   ```

2. **Найди свой сервис:**
   - Кликни на проект TTFD

3. **Запусти деплой вручную:**
   - Нажми кнопку **"Manual Deploy"**
   - Выбери ветку **"main"**
   - Нажми **"Deploy"**

4. **Дождись завершения:**
   - Статус изменится на "Live"
   - Обычно занимает 2-5 минут

---

### Вариант 2: Настройка автодеплоя

#### Проверь настройки:

1. **Открой настройки сервиса:**
   - Dashboard → Твой сервис → Settings

2. **Проверь "Auto-Deploy":**
   - Должно быть: **"Yes"**
   - Ветка: **"main"**

3. **Если Auto-Deploy выключен:**
   - Включи его в настройках
   - Сохрани изменения

#### Проверь webhook:

1. **В Render:**
   - Settings → Build & Deploy
   - Скопируй Deploy Hook URL

2. **В GitHub:**
   - Репозиторий → Settings → Webhooks
   - Проверь наличие webhook от Render
   - URL должен быть: `https://api.render.com/deploy/...`

3. **Если webhook нет:**
   - Add webhook
   - Payload URL: вставь Deploy Hook URL из Render
   - Content type: `application/json`
   - Events: "Just the push event"
   - Active: ✅

---

### Вариант 3: Через Render CLI

```bash
# Установи Render CLI
npm install -g @render/cli

# Войди в аккаунт
render login

# Запусти деплой
render deploy --service=TTFD
```

---

## 🔍 Проверка текущего деплоя

### На Render:

1. **Проверь последний коммит:**
   - Dashboard → Твой сервис
   - Смотри "Latest Deploy"
   - Должен быть коммит: `e273ee7`

2. **Если коммит старый:**
   - Значит автодеплой не работает
   - Используй ручной деплой

### На GitHub:

1. **Проверь последний коммит:**
   ```
   https://github.com/1HaveNotSoul/TTFD/commits/main
   ```

2. **Последний коммит должен быть:**
   ```
   e273ee7 - feat: добавлена система тикетов...
   ```

---

## 🚀 Быстрое решение (прямо сейчас)

### Шаг 1: Открой Render
```
https://dashboard.render.com
```

### Шаг 2: Найди TTFD проект

### Шаг 3: Нажми "Manual Deploy" → "Deploy latest commit"

### Шаг 4: Дождись завершения (2-5 минут)

### Шаг 5: Проверь сайт
```
https://ttfd.onrender.com
```

---

## 📋 Что должно обновиться

После деплоя на сайте появится:

1. ✅ **Страница входа:**
   - Только кнопка "Войти через Discord"
   - Убрана форма логин/пароль

2. ✅ **Главная страница:**
   - Кликабельные профили онлайн пользователей
   - При клике открывается профиль

3. ✅ **API endpoints:**
   - `/api/user_by_discord/<id>` - новый endpoint

4. ✅ **Telegram бот:**
   - Показывает реальную статистику БД
   - Админ-панель с просмотром БД

---

## ⚠️ Возможные проблемы

### 1. Деплой падает с ошибкой

**Проверь логи:**
- Dashboard → Твой сервис → Logs
- Смотри ошибки в процессе деплоя

**Частые причины:**
- Отсутствуют зависимости в `requirements.txt`
- Ошибки в коде (синтаксис)
- Проблемы с переменными окружения

### 2. Сайт не обновляется после деплоя

**Очисти кеш браузера:**
- Ctrl + Shift + R (Windows)
- Cmd + Shift + R (Mac)

**Или открой в режиме инкогнито:**
- Ctrl + Shift + N

### 3. Auto-Deploy не работает

**Проверь:**
1. GitHub webhook активен
2. Render имеет доступ к репозиторию
3. Auto-Deploy включен в настройках

**Пересоздай webhook:**
- Удали старый в GitHub
- Создай новый с Deploy Hook URL из Render

---

## 🔧 Настройка переменных окружения

Убедись что на Render установлены все переменные:

```env
DISCORD_TOKEN=твой_токен
DISCORD_CLIENT_ID=client_id
DISCORD_CLIENT_SECRET=client_secret
DISCORD_REDIRECT_URI=https://ttfd.onrender.com/auth/discord/callback
SECRET_KEY=секретный_ключ
WEB_PORT=5000
TELEGRAM_BOT_TOKEN=токен (если используешь)
TELEGRAM_ADMIN_IDS=id1,id2 (если используешь)
```

**Важно:** `DISCORD_REDIRECT_URI` должен указывать на твой Render домен!

---

## 📊 Проверка после деплоя

### 1. Проверь главную страницу:
```
https://ttfd.onrender.com
```

### 2. Проверь страницу входа:
```
https://ttfd.onrender.com/login
```
Должна быть только кнопка Discord.

### 3. Проверь API:
```
https://ttfd.onrender.com/api/stats
```
Должен вернуть JSON со статистикой.

### 4. Проверь онлайн пользователей:
```
https://ttfd.onrender.com/api/online
```

---

## 🎯 Итоговый чеклист

- [ ] Открыл Render Dashboard
- [ ] Нашел проект TTFD
- [ ] Нажал "Manual Deploy"
- [ ] Дождался завершения деплоя
- [ ] Очистил кеш браузера
- [ ] Проверил сайт - изменения применились
- [ ] Проверил Auto-Deploy включен
- [ ] Проверил GitHub webhook активен

---

## 📞 Если ничего не помогло

1. **Проверь логи деплоя на Render**
2. **Проверь что коммит `e273ee7` на GitHub**
3. **Попробуй пересоздать сервис на Render:**
   - New → Web Service
   - Подключи репозиторий заново
   - Настрой переменные окружения

---

**Дата:** 29 января 2026  
**Последний коммит:** `e273ee7`  
**Статус:** Готов к деплою ✅
