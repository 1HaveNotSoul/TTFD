# 🎫 Тикет-система и команда очистки

## ✅ Что добавлено:

### 1. 🎫 Тикет-система
- Команда `!ticket` для создания тикета
- Автоматическое создание приватного канала
- Доступ только для создателя и поддержки
- Команда `!close` для закрытия тикета
- Поддержка ролей (ADMIN, MODERATOR, SUPPORT)

### 2. 🧹 Команда очистки чата
- Команда `!clear <число>` для удаления сообщений
- Максимум 100 сообщений за раз (ограничение Discord)
- Доступна только с правом "Manage Messages"
- Автоудаление уведомления через 5 секунд
- Полная обработка ошибок

---

## 📋 Команды:

### Тикет-система:

#### `!ticket`
Создать тикет поддержки

**Что происходит:**
1. Создаётся приватный канал `ticket-username-1`
2. Канал доступен только тебе и поддержке
3. Бот отправляет приветственное сообщение
4. Поддержка получает уведомление

**Пример:**
```
!ticket
```

**Результат:**
```
✅ Тикет создан: #ticket-NxtTreep-1
```

---

#### `!close`
Закрыть тикет (только в канале тикета)

**Кто может закрыть:**
- Создатель тикета
- Пользователи с ролями: ADMIN, MODERATOR, SUPPORT
- Администраторы сервера

**Что происходит:**
1. Отправляется сообщение о закрытии
2. Через 5 секунд канал удаляется
3. Тикет убирается из списка активных

**Пример:**
```
!close
```

**Результат:**
```
🔒 Тикет закрывается...
Канал будет удалён через 5 секунд.
```

---

### Очистка чата:

#### `!clear <число>`
Удалить сообщения в канале

**Требования:**
- Право "Manage Messages" (Управление сообщениями)
- Число от 1 до 100

**Примеры:**
```
!clear 10    # Удалить 10 сообщений
!clear 50    # Удалить 50 сообщений
!clear 100   # Удалить 100 сообщений (максимум)
```

**Результат:**
```
🧹 Чат очищен
Удалено 50 сообщений
```
*(Сообщение автоудаляется через 5 секунд)*

---

## 🔐 Права доступа:

### Тикеты:

**Создать тикет:**
- Любой пользователь сервера

**Закрыть тикет:**
- Создатель тикета
- Роли: ADMIN, MODERATOR, SUPPORT
- Администраторы сервера

**Видеть тикеты:**
- Создатель тикета
- Роли: ADMIN, MODERATOR, SUPPORT
- Администраторы сервера
- Бот

### Очистка чата:

**Использовать !clear:**
- Право "Manage Messages"
- Обычно: модераторы и администраторы

---

## ⚙️ Настройка ролей поддержки:

В файле `bot.py` найди строку:
```python
SUPPORT_ROLES = ['ADMIN', 'MODERATOR', 'SUPPORT', 'Администратор', 'Модератор']
```

Добавь свои роли:
```python
SUPPORT_ROLES = ['ADMIN', 'MODERATOR', 'SUPPORT', 'Администратор', 'Модератор', 'Хелпер', 'Саппорт']
```

---

## 🎨 Как работает тикет-система:

### Создание тикета:

1. Пользователь пишет `!ticket`
2. Бот проверяет что у него нет открытого тикета
3. Бот создаёт/находит категорию "🎫 Тикеты"
4. Создаётся канал `ticket-username-номер`
5. Настраиваются права доступа:
   - ❌ @everyone - не видит
   - ✅ Создатель - видит и пишет
   - ✅ Поддержка - видит и пишет
   - ✅ Бот - управляет каналом
6. Отправляется приветственное сообщение
7. Уведомляется поддержка

### Закрытие тикета:

1. Пользователь/поддержка пишет `!close`
2. Бот проверяет что это канал тикета
3. Бот проверяет права
4. Отправляется сообщение о закрытии
5. Через 5 секунд канал удаляется
6. Тикет убирается из списка активных

---

## 🐛 Обработка ошибок:

### Тикеты:

**"У тебя уже есть открытый тикет"**
- Закрой существующий тикет командой `!close`
- Или попроси поддержку закрыть его

**"Не удалось создать категорию"**
- Проверь что у бота есть право "Manage Channels"
- Проверь что на сервере не достигнут лимит каналов

**"У тебя нет прав закрыть этот тикет"**
- Только создатель или поддержка могут закрыть тикет
- Попроси поддержку закрыть тикет

### Очистка:

**"Укажи количество сообщений"**
- Используй: `!clear 50`
- Не используй: `!clear`

**"Укажи корректное число"**
- Используй число: `!clear 50`
- Не используй текст: `!clear много`

**"Максимум можно удалить 100 сообщений"**
- Это ограничение Discord API
- Используй команду несколько раз для большего количества

**"У тебя нет прав на управление сообщениями"**
- Нужна роль с правом "Manage Messages"
- Обратись к администратору

---

## 📊 Статистика:

### Что отслеживается:

- Количество созданных тикетов
- Активные тикеты (в памяти бота)
- Количество очищенных сообщений (в логах)

### Логи:

Все действия логируются в консоль:
```
✅ Создан тикет: ticket-NxtTreep-1 для NxtTreep
✅ Тикет закрыт: ticket-NxtTreep-1
✅ Очищено 50 сообщений в #general пользователем Admin
```

---

## 🚀 Деплой:

1. Код уже обновлён в `bot.py`
2. Загрузи на GitHub:
```bash
cd Desktop/папки/TTFD-Website
git add bot.py TICKET_SYSTEM_GUIDE.md
git commit -m "Add ticket system and clear command"
git push
```

3. Render автоматически обновит бота

4. Проверь что у бота есть права:
   - ✅ Manage Channels (для создания каналов)
   - ✅ Manage Messages (для очистки)
   - ✅ Read Messages
   - ✅ Send Messages
   - ✅ Embed Links

---

## ✨ Готово!

Теперь у тебя:
- ✅ Полноценная тикет-система
- ✅ Команда очистки чата
- ✅ Поддержка ролей
- ✅ Обработка всех ошибок
- ✅ Красивые embed сообщения
- ✅ Автоудаление уведомлений

**Команды:**
- `!ticket` - создать тикет
- `!close` - закрыть тикет
- `!clear <число>` - очистить чат

Используй `!help` чтобы увидеть все команды! 🎉
