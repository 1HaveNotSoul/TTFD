# 🗄️ Настройка PostgreSQL для постоянного хранения данных

## Зачем нужна PostgreSQL?

На Render Free плане файлы (включая `user_data.json`) удаляются при каждом деплое. PostgreSQL решает эту проблему - данные хранятся постоянно.

## 📋 Шаг 1: Создай PostgreSQL базу на Render

1. Зайди на https://dashboard.render.com
2. Нажми **"New +"** → **"PostgreSQL"**
3. Заполни:
   - **Name:** `ttfd-database` (или любое другое)
   - **Database:** `ttfd_db`
   - **User:** `ttfd_user`
   - **Region:** Frankfurt (EU Central)
   - **Plan:** **Free** (0$)
4. Нажми **"Create Database"**
5. Подожди 2-3 минуты пока создастся

## 📋 Шаг 2: Получи DATABASE_URL

После создания базы:

1. Открой созданную базу данных
2. Найди раздел **"Connections"**
3. Скопируй **"Internal Database URL"** (начинается с `postgres://`)

Пример:
```
postgres://ttfd_user:password123@dpg-xxxxx.frankfurt-postgres.render.com/ttfd_db
```

## 📋 Шаг 3: Добавь DATABASE_URL в Web Service

1. Открой свой Web Service (ttfd-bot)
2. Перейди в **Environment**
3. Нажми **"Add Environment Variable"**
4. Добавь:
   - **Key:** `DATABASE_URL`
   - **Value:** (вставь скопированный Internal Database URL)
5. Нажми **"Save Changes"**

Render автоматически перезапустит сервис!

## 📋 Шаг 4: Обнови requirements.txt

Добавь psycopg2 в зависимости:

```txt
discord.py>=2.3.0
flask>=3.0.0
python-dotenv>=1.0.0
aiohttp>=3.9.0
psycopg2-binary>=2.9.0
```

## 📋 Шаг 5: Обнови код

Замени импорт в файлах:

**В `bot.py`, `web.py`, `main.py`:**

Было:
```python
from database import db
```

Стало:
```python
# Пытаемся использовать PostgreSQL, если нет - JSON
try:
    from database_postgres import db
    print("✅ Используется PostgreSQL")
except:
    from database import db
    print("⚠️ Используется JSON файл")
```

## 📋 Шаг 6: Загрузи на GitHub

```bash
cd C:\Users\brawl\OneDrive\Desktop\папки\TTFD-Website
git add .
git commit -m "Add PostgreSQL support"
git push
```

Render автоматически задеплоит с новой БД!

## ✅ Проверка

После деплоя проверь логи в Render Dashboard:

**Успешно:**
```
✅ Таблицы PostgreSQL инициализированы
✅ Используется PostgreSQL
```

**Ошибка:**
```
⚠️ DATABASE_URL не установлен, используется JSON файл
```

Если видишь ошибку - проверь что DATABASE_URL добавлен в Environment Variables.

## 🎯 Преимущества PostgreSQL:

✅ Данные не теряются при деплое
✅ Быстрее чем JSON файлы
✅ Поддержка одновременных запросов
✅ Бесплатно на Render (до 1GB)
✅ Автоматические бэкапы

## 🔄 Миграция данных

Если у тебя уже есть данные в JSON файле:

1. Скачай `user_data.json` и `accounts.json` с Render (если они есть)
2. Создай скрипт миграции (могу помочь)
3. Загрузи данные в PostgreSQL

## 📊 Управление базой данных

### Подключение к БД:

Render предоставляет **PSQL Command** для подключения:

1. Открой базу данных в Render Dashboard
2. Найди **"PSQL Command"**
3. Скопируй и выполни в терминале

### Полезные SQL команды:

```sql
-- Посмотреть всех пользователей
SELECT * FROM users ORDER BY xp DESC LIMIT 10;

-- Посмотреть количество пользователей
SELECT COUNT(*) FROM users;

-- Посмотреть топ по кликам
SELECT username, clicks, xp FROM users ORDER BY clicks DESC LIMIT 5;

-- Очистить таблицу (осторожно!)
TRUNCATE TABLE users;
```

## 🚨 Важно!

- **Free план PostgreSQL:** 1GB хранилища, достаточно для тысяч пользователей
- **Бэкапы:** Render делает автоматически
- **Безопасность:** Используй Internal Database URL (не External)
- **Производительность:** Намного быстрее JSON файлов

## 💡 Альтернативы PostgreSQL:

Если не хочешь использовать PostgreSQL:

1. **MongoDB Atlas** - бесплатно 512MB
2. **Supabase** - бесплатно 500MB + удобный интерфейс
3. **PlanetScale** - бесплатно 5GB (MySQL)
4. **CockroachDB** - бесплатно 5GB

Но PostgreSQL на Render - самый простой вариант для начала!

## 📞 Нужна помощь?

Если что-то не работает:
1. Проверь логи в Render Dashboard
2. Убедись что DATABASE_URL правильный
3. Проверь что psycopg2-binary в requirements.txt
4. Пришли ошибку - помогу разобраться!
