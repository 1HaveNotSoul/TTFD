# 🎨 Премиум настройки - Руководство

## 📋 Обзор

Создана улучшенная версия страницы настроек с современным дизайном и дополнительными функциями.

---

## ✨ Новые возможности

### 1. Сеточный макет
- Карточки настроек расположены в 2 колонки на десктопе
- Автоматическая адаптация под размер экрана
- Более компактное и удобное расположение

### 2. Улучшенная загрузка файлов
- Визуальная индикация выбранного файла
- Отображение размера файла
- Проверка размера перед загрузкой:
  - Аватарка: максимум 5MB
  - Музыка: максимум 10MB

### 3. Раздельные формы
- Основная информация (имя, email, логин)
- Биография (отдельная форма с счётчиком символов)
- Аватарка (с превью)
- Музыка (с плеером)
- Discord (с валидацией ID)
- Пароль (с проверкой совпадения)

### 4. Валидация
- Проверка размера файлов
- Проверка длины Discord ID (минимум 17 символов)
- Проверка длины пароля (минимум 6 символов)
- Проверка совпадения паролей
- Ограничение биографии (500 символов)

### 5. Улучшенные уведомления
- Иконки в сообщениях (✅ для успеха)
- Плавная анимация появления
- Автоматическое скрытие через 5 секунд
- Прокрутка к сообщению

---

## 🎨 Дизайн

### Использует единую дизайн-систему v3.0
- Фиолетовый градиентный фон
- Белые карточки с тенями
- Градиентные кнопки
- Единые отступы и шрифты

### Дополнительные стили
```css
.settings-grid - сетка 2 колонки
.file-upload - стилизованная загрузка файлов
.preview - превью аватарки и музыки
.btn-group - группа кнопок
```

---

## 📱 Адаптивность

### Десктоп (> 768px)
- 2 колонки карточек
- Полная ширина форм
- Все функции доступны

### Мобильные (< 768px)
- 1 колонка карточек
- Вертикальное расположение
- Адаптированные размеры

---

## 🔧 Использование

### Подключение
```html
<!-- Вместо settings.html используй settings_premium.html -->
<a href="/settings_premium">Настройки</a>
```

### Маршрут в web.py
```python
@app.route('/settings_premium')
@login_required
def settings_premium():
    return render_template('settings_premium.html', current_user=current_user)
```

---

## 📋 Структура форм

### 1. Основная информация
```html
<form id="profileForm">
  - display_name (текст, обязательно)
  - email (email, обязательно)
  - username (текст, disabled)
</form>
```

### 2. Биография
```html
<form id="bioForm">
  - bio (textarea, макс 500 символов)
</form>
```

### 3. Аватарка
```html
<form id="avatarForm">
  - avatar_file (file, image/*, макс 5MB)
  - превью текущей аватарки
</form>
```

### 4. Музыка
```html
<form id="musicForm">
  - music_file (file, audio/mp3, макс 10MB)
  - плеер текущей музыки
</form>
```

### 5. Discord
```html
<form id="discordForm">
  - discord_id (текст, мин 17 символов)
</form>
```

### 6. Пароль
```html
<form id="passwordForm">
  - old_password (password, обязательно)
  - new_password (password, мин 6 символов)
  - new_password2 (password, мин 6 символов)
</form>
```

---

## 🔌 API Endpoints

### Обновление профиля
```javascript
POST /api/update_profile
Body: {
  display_name: string,
  email: string,
  bio: string
}
Response: {
  success: boolean,
  error?: string
}
```

### Загрузка аватарки
```javascript
POST /api/upload_avatar
Body: FormData {
  avatar: File (макс 5MB)
}
Response: {
  success: boolean,
  avatar_url?: string,
  error?: string
}
```

### Загрузка музыки
```javascript
POST /api/upload_music
Body: FormData {
  music: File (макс 10MB)
}
Response: {
  success: boolean,
  music_url?: string,
  error?: string
}
```

### Привязка Discord
```javascript
POST /api/link_discord
Body: {
  discord_id: string (мин 17 символов)
}
Response: {
  success: boolean,
  error?: string
}
```

### Смена пароля
```javascript
POST /api/change_password
Body: {
  old_password: string,
  new_password: string (мин 6 символов)
}
Response: {
  success: boolean,
  error?: string
}
```

---

## ✅ Преимущества

### По сравнению с обычной версией:
1. **Компактнее** - 2 колонки вместо 1
2. **Удобнее** - раздельные формы для каждой секции
3. **Безопаснее** - валидация на клиенте
4. **Информативнее** - показывает размер файлов
5. **Красивее** - использует единую дизайн-систему v3.0

---

## 🐛 Обработка ошибок

### Клиентская валидация
- Проверка размера файлов перед загрузкой
- Проверка длины Discord ID
- Проверка длины пароля
- Проверка совпадения паролей
- Ограничение длины биографии

### Серверная валидация
- Проверка типа файлов
- Проверка размера файлов
- Проверка прав доступа
- Проверка существования пользователя

### Сообщения об ошибках
```javascript
showMessage('Текст ошибки', 'error');  // Красное сообщение
showMessage('Текст успеха', 'success'); // Зелёное сообщение
```

---

## 📝 Примеры использования

### Обновление имени и email
```javascript
// Заполни поля
document.getElementById('display_name').value = 'Новое имя';
document.getElementById('email').value = 'new@email.com';

// Отправь форму
document.getElementById('profileForm').submit();
```

### Загрузка аватарки
```javascript
// Выбери файл через input
const fileInput = document.getElementById('avatar_file');
fileInput.click();

// После выбора файла отправь форму
document.getElementById('avatarForm').submit();
```

### Привязка Discord
```javascript
// Введи Discord ID
document.getElementById('discord_id').value = '123456789012345678';

// Отправь форму
document.getElementById('discordForm').submit();
```

---

## 🎯 Рекомендации

### Для пользователей
1. Используй изображения до 5MB для аватарки
2. Используй MP3 файлы до 10MB для музыки
3. Узнай свой Discord ID в настройках Discord
4. Используй надёжные пароли (минимум 6 символов)

### Для разработчиков
1. Проверяй размер файлов на сервере
2. Сохраняй файлы в безопасном месте
3. Используй уникальные имена файлов
4. Добавь rate limiting для загрузки файлов
5. Добавь антивирусную проверку файлов

---

## 🚀 Будущие улучшения

### Планируется добавить:
- [ ] Обрезка и изменение размера аватарки
- [ ] Предпросмотр аватарки перед загрузкой
- [ ] Drag & drop для загрузки файлов
- [ ] Прогресс-бар загрузки
- [ ] Множественная загрузка файлов
- [ ] Интеграция с другими соцсетями
- [ ] Двухфакторная аутентификация
- [ ] История изменений профиля

---

## 📚 Связанные документы

- [DESIGN_COMPLETE.md](DESIGN_COMPLETE.md) - Дизайн-система
- [FRONTEND_AUDIT_COMPLETE.md](FRONTEND_AUDIT_COMPLETE.md) - Frontend аудит
- [TESTING_CHECKLIST.md](TESTING_CHECKLIST.md) - Чек-лист тестирования

---

**Создано:** 29 января 2026  
**Версия:** 1.0  
**CSS версия:** 3.0  
**Статус:** ✅ ГОТОВО
