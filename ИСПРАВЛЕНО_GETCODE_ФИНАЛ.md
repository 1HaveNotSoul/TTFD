# ✅ ИСПРАВЛЕНО: Команда /getcode теперь работает

## 🔧 Что было исправлено

### 1. Убран лишний импорт asyncpg
**Проблема:** В `discord_link_code.py` импортировался `asyncpg`, но не использовался
**Решение:** Удалён импорт `asyncpg` и неиспользуемые модули (`os`, `string`)

### 2. Изменён метод синхронизации команд
**Проблема:** `403 Forbidden (error code: 50001): Missing Access` при guild sync
**Решение:** 
- Используем **GLOBAL SYNC** как основной метод (работает везде)
- Guild sync как дополнительный (для мгновенного появления на сервере)
- Если guild sync не работает - команды всё равно появятся через global sync

## 📝 Изменённые файлы

### `TTFD/discord-bot/py/discord_link_code.py`
```python
# БЫЛО:
import os
import string
import asyncpg  # ❌ Не используется

# СТАЛО:
# Убраны неиспользуемые импорты
```

### `TTFD/discord-bot/py/bot.py`
```python
# БЫЛО:
guild = discord.Object(id=config.GUILD_ID)
synced = await bot.tree.sync(guild=guild)  # ❌ 403 ошибка

# СТАЛО:
synced = await bot.tree.sync()  # ✅ Global sync (работает везде)
try:
    guild = discord.Object(id=config.GUILD_ID)
    synced_guild = await bot.tree.sync(guild=guild)  # Дополнительно
except:
    pass  # Не критично если не работает
```

## 🚀 Что дальше

### 1. Залить изменения на GitHub
```bash
cd TTFD
git add .
git commit -m "fix: исправлена регистрация команды /getcode (убран asyncpg, добавлен global sync)"
git push origin main
```

### 2. Дождаться деплоя на Railway
Railway автоматически задеплоит изменения из GitHub (1-2 минуты)

### 3. Проверить логи Railway
Должны увидеть:
```
✅ Команды привязки через код зарегистрированы (/getcode, /checklink, /unlink)
✅ Синхронизировано X slash команд с Discord (global sync)
⏰ Команды появятся в течение 1 часа
```

### 4. Проверить команду в Discord
- Команда `/getcode` появится в течение **1 часа** (global sync)
- Если guild sync сработал - появится **мгновенно**

## 📊 Как работает система

### Discord бот (генерирует код)
1. Пользователь: `/getcode`
2. Бот генерирует код (6 символов, 3 минуты)
3. Код сохраняется в `json/user_data.json`
4. Код отправляется в ЛС пользователю

### Telegram бот (использует код)
1. Пользователь: `/code ABC123`
2. Бот читает `discord-bot/json/user_data.json`
3. Проверяет код (существует, не истёк, не использован)
4. Помечает код как использованный
5. Сохраняет привязку в обеих БД

## ⚠️ Важно

### Путь к Discord БД в Telegram боте
Текущий путь: `../../discord-bot/json/user_data.json`

**На Railway может не работать!** Если структура папок другая, нужно:
1. Использовать переменную окружения `DISCORD_DB_PATH`
2. Или использовать PostgreSQL вместо JSON

### Альтернатива: PostgreSQL
Если JSON не работает на Railway, можно:
1. Создать таблицу `link_codes` в PostgreSQL
2. Оба бота будут читать/писать в одну таблицу
3. Не нужно читать файлы друг друга

## 🎯 Следующие шаги

1. ✅ Залить на GitHub
2. ⏰ Дождаться деплоя (1-2 минуты)
3. 📋 Проверить логи Railway
4. ⏰ Подождать до 1 часа (global sync)
5. 🎮 Протестировать `/getcode` в Discord
6. 📱 Протестировать `/code` в Telegram

## 🔍 Как проверить что всё работает

### В логах Railway (Discord бот)
```
✅ Команды привязки через код зарегистрированы
✅ Синхронизировано X slash команд (global sync)
⏰ Команды появятся в течение 1 часа
```

### В Discord
```
/getcode → Код отправлен в ЛС
```

### В Telegram
```
/code ABC123 → ✅ Аккаунты привязаны!
```

---

**Дата:** 09.02.2026
**Статус:** ✅ Готово к деплою
