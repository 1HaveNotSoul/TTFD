# ⚡ Быстрый старт: Интеграция платформ

**3 команды для полной интеграции**

---

## 1️⃣ Применить миграцию (1 минута)

```bash
psql $DATABASE_URL -f TTFD/shared/migration_unified.sql
```

---

## 2️⃣ Мигрировать данные (5 минут)

```bash
cd TTFD/shared
python migrate_to_unified.py
```

---

## 3️⃣ Проверить результат (1 минута)

```sql
psql $DATABASE_URL -c "SELECT COUNT(*) FROM unified_users;"
psql $DATABASE_URL -c "SELECT * FROM unified_users LIMIT 5;"
```

---

## ✅ Готово!

Теперь обнови код платформ по инструкции:
📄 `TTFD/СЛЕДУЮЩИЕ_ШАГИ_ИНТЕГРАЦИЯ.md`

---

## 🔍 Быстрая проверка

```python
# Проверка подключения
import asyncio
import sys
sys.path.append('TTFD/shared')
from database_unified import get_unified_db

async def test():
    db = await get_unified_db()
    users = await db.get_leaderboard(5)
    print(f"✅ Найдено {len(users)} пользователей")
    for user in users:
        print(f"  - {user.display_name}: {user.xp} XP")
    await db.disconnect()

asyncio.run(test())
```

---

## 📊 Что дальше?

1. **Telegram Bot** - интегрировать unified_integration
2. **Discord Bot** - интегрировать unified_integration
3. **Website** - интегрировать unified_integration
4. **Sync Worker** - запустить для синхронизации

Подробности: `TTFD/СЛЕДУЮЩИЕ_ШАГИ_ИНТЕГРАЦИЯ.md`
